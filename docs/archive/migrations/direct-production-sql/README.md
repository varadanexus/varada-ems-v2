# Archived direct-production SQL

These files record SQL that was previously run directly against production.
They are retained for audit and recovery only.

They are not part of the active migration chain and must not be copied back,
renamed into, or executed from `new-ems/supabase/migrations`. Equivalent live
state is represented by the production database and subsequent canonical
migrations.

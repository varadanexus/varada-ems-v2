import { MODULES, ROUTES, TOAST_TYPES, WORKSPACES } from "../config/constants.js";
import { getSupabaseClient } from "../config/supabase.js";
import { bootstrapProtectedPage, renderModuleContent } from "./layout.js";
import { hasAnyRolePermission } from "./permissions.js";
import { PERMISSIONS } from "../config/roles.js";
import { deleteHospitalBillingDocumentFromDrive, deleteHospitalProjectDocumentFromDrive, ensureHospitalProjectDriveFolders, uploadHospitalProjectDocumentToDrive, uploadHospitalVendorInvoiceToDrive } from "./drive-api.js";
import { logAuditEvent } from "./audit.js";
import { showToast } from "./utils.js";
import { listEmailHistory, sendModuleEmail } from "./email-api.js";
import { listWhatsAppHistory, sendWhatsAppWorkspaceMessage } from "./whatsapp-api.js";
import { downloadHospitalProcurementProposal, downloadHospitalPurchaseOrder } from "./hospital-procurement-pdf.js?v=procurement-charge-gst-1";
import { archiveHospitalBillingPdf, buildHospitalBillingPdf, downloadHospitalBillingPdf } from "./hospital-billing-pdf.js?v=hospital-billing-2";
import { DEFAULT_QUOTATION_TERMS, downloadHospitalQuotationPdf } from "./hospital-quotation-pdf.js?v=quotation-1";

const db = getSupabaseClient();
const projectId = new URLSearchParams(location.search).get("project_id") || "";
const SERVICE_SCOPES = {
  hospital_construction: "Hospital Construction", hospital_consultancy: "Hospital Consultancy",
  licensing_regulatory: "Licensing & Regulatory", medical_equipment_procurement: "Medical Equipment Procurement",
  interior_design: "Interior Design", hospital_renovation: "Hospital Renovation",
  medical_gas_pipeline: "Medical Gas Pipeline", modular_ot: "Modular OT", cssd: "CSSD",
  icu_setup: "ICU Setup", laboratory_setup: "Laboratory Setup", radiology_setup: "Radiology Setup",
  nabh_accreditation: "NABH Accreditation", pmc: "PMC", turnkey_project: "Turnkey Project"
};
const PAGE_META = {
  overview: ["Project Overview", "Project scope, delivery status and commercial summary"],
  planning: ["Planning & Design", "Planning, design and consultancy work packages"],
  construction: ["Construction", "Civil, MEP, interiors and site execution scopes"],
  procurement: ["Procurement", "Project procurement requirements and vendor sourcing"],
  "medical-equipment": ["Medical Equipment", "Medical equipment sourcing, delivery, installation and commissioning"],
  licensing: ["Licensing & Compliance", "Regulatory approvals, accreditation and statutory compliance"],
  contractors: ["Contractors", "Project-specific contractor and specialist vendor assignments"],
  documents: ["Documents", "Google Drive project archive and document upload"],
  quotations: ["Quotations", "Branded client quotations with itemised GST, discounts and terms & conditions"],
  "client-payments": ["Billing", "Advanced invoices, credit notes and Central Accounts status"],
  "vendor-payments": ["Vendor Payments", "Project vendor bill creation and payment-status register"],
  communications: ["Communications", "Project query history with Email and WhatsApp service access"],
  reports: ["Project Reports", "Delivery, billing, cost and margin reporting"],
  settings: ["Project Settings", "Project status, dates, progress and service scope configuration"]
};
const ROUTE_BY_VIEW = {
  overview: ROUTES.HOSPITAL_PROJECT_OVERVIEW, planning: ROUTES.HOSPITAL_PROJECT_PLANNING,
  construction: ROUTES.HOSPITAL_PROJECT_CONSTRUCTION, procurement: ROUTES.HOSPITAL_PROJECT_PROCUREMENT,
  "medical-equipment": ROUTES.HOSPITAL_PROJECT_MEDICAL_EQUIPMENT, licensing: ROUTES.HOSPITAL_PROJECT_LICENSING,
  contractors: ROUTES.HOSPITAL_PROJECT_CONTRACTORS, documents: ROUTES.HOSPITAL_PROJECT_DOCUMENTS,
  quotations: ROUTES.HOSPITAL_PROJECT_QUOTATIONS,
  "client-payments": ROUTES.HOSPITAL_PROJECT_CLIENT_PAYMENTS, "vendor-payments": ROUTES.HOSPITAL_PROJECT_VENDOR_PAYMENTS,
  communications: ROUTES.HOSPITAL_PROJECT_COMMUNICATIONS, reports: ROUTES.HOSPITAL_PROJECT_REPORTS,
  settings: ROUTES.HOSPITAL_PROJECT_SETTINGS
};
const state = { view: "overview", boot: null, project: null, scopes: [], packages: [], items: [], licenses: [], vendors: [], assignments: [], invoices: [], invoiceLines: [], credits: [], vendorBills: [], queries: [], messages: [], documents: [], emails: [], whatsapp: [], whatsappContactHistory: [], communicationModal: null, vendorBillModal: false, procurementPackages: [], procurementLines: [], procurementCharges: [], procurementTaxes: [], procurementApprovals: [], purchaseOrders: [], activeProcurementPackageId: null, procurementPackageEditingId: null, procurementChargeEditingId: null, procurementWizardStep: 1, procurementTermsByPackage: {}, equipmentEditingId: null, billingEditingId: null, creditEditingId: null, billingModal: null, billingPreview: null, quotations: [], quotationLines: [], quotationEditingId: null, quotationModal: false, quotationPreview: null };

const esc = (value) => String(value ?? "").replace(/[&<>"']/g, (char) => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;", "'": "&#39;" }[char]));
const label = (value) => String(value || "—").replaceAll("_", " ");
const money = (value) => new Intl.NumberFormat("en-IN", { style: "currency", currency: "INR", maximumFractionDigits: 0 }).format(Number(value) || 0);
const date = (value) => value ? new Date(value).toLocaleDateString("en-IN", { day: "2-digit", month: "short", year: "numeric" }) : "—";
const normalizePhone = (value) => { const digits=String(value||"").replace(/\D/g,"");return digits.length===10?`91${digits}`:digits.startsWith("0")&&digits.length===11?`91${digits.slice(1)}`:digits; };
const projectContact = () => { const client=state.project?.hospital_clients||{};return { name:client.authorized_representative_name||client.contact_name||client.hospital_name||"Hospital client", email:client.email||client.authorized_representative_email||client.billing_email||client.portal_email||"", phone:client.whatsapp_phone||client.authorized_representative_whatsapp||client.phone||client.authorized_representative_phone||client.portal_phone||client.billing_phone||"" }; };
function procurementItemAmounts(item) { const quantity=Number(item.quantity)||0,unitPrice=Number(item.unitPrice)||0,discount=Number(item.discount)||0,gstRate=Number(item.gstRate)||0,unitsTotal=quantity*unitPrice,enteredTotal=unitsTotal-discount;const taxable=Number.isFinite(Number(item.taxableAmount))?Number(item.taxableAmount):(item.gstIncluded&&gstRate>0?enteredTotal*100/(100+gstRate):enteredTotal);const gst=item.gstIncluded?enteredTotal-taxable:taxable*gstRate/100;return { unitsTotal, gst, totalIncGst: taxable+gst }; }
const route = (view) => `${ROUTE_BY_VIEW[view]}?project_id=${encodeURIComponent(projectId)}`;
const can = (action) => hasAnyRolePermission(state.boot?.roleCodes || [], MODULES.HOSPITAL_PROJECTS, action, { allowedModules: state.boot?.allowedModules || [] });
const hasScope = (...codes) => state.scopes.includes("turnkey_project") || codes.some((code) => state.scopes.includes(code));

function workspaceItems() {
  const items = [{ view: "overview", label: "Overview", show: true }];
  items.push({ view: "planning", label: "Planning & Design", show: hasScope("hospital_construction", "hospital_consultancy", "interior_design", "hospital_renovation", "medical_gas_pipeline", "modular_ot", "cssd", "icu_setup", "laboratory_setup", "radiology_setup", "pmc") });
  items.push({ view: "construction", label: "Construction", show: hasScope("hospital_construction", "interior_design", "hospital_renovation", "medical_gas_pipeline", "modular_ot", "pmc") });
  items.push({ view: "procurement", label: "Procurement", show: hasScope("medical_equipment_procurement", "modular_ot", "cssd", "icu_setup", "laboratory_setup", "radiology_setup") });
  items.push({ view: "licensing", label: "Licensing & Compliance", show: hasScope("licensing_regulatory", "nabh_accreditation") });
  items.push({ view: "contractors", label: "Contractors", show: hasScope("hospital_construction", "interior_design", "hospital_renovation", "medical_gas_pipeline", "modular_ot", "pmc") });
  items.push({ view: "documents", label: "Documents", show: true }, { view: "quotations", label: "Quotations", show: true }, { view: "client-payments", label: "Billing", show: true });
  items.push({ view: "vendor-payments", label: "Vendor Payments", show: true });
  items.push({ view: "communications", label: "Communications", show: true }, { view: "reports", label: "Reports", show: true });
  return items.filter((item) => item.show);
}

function sidebarContext(project) {
  return {
    title: project.title,
    subtitle: project.project_code,
    showBack: false,
    sections: [
      { title: "Dashboard", items: [
        { module: MODULES.HOSPITAL_PROJECTS, label: "Command Center", href: ROUTES.DASHBOARD },
        { module: MODULES.HOSPITAL_PROJECTS, label: "Hospital Projects", href: ROUTES.HOSPITAL_PROJECTS },
        { module: MODULES.HOSPITAL_PROJECTS, label: "Project Dashboard (Overview)", href: route("overview") }
      ] },
      { title: "Project Workspace", items: workspaceItems().filter((item) => item.view !== "overview").map((item) => ({ module: MODULES.HOSPITAL_PROJECTS, label: item.label, href: route(item.view) })) },
      { title: "Administration", items: [{ module: MODULES.HOSPITAL_PROJECTS, label: "Project Settings", href: route("settings") }] }
    ]
  };
}

async function query(table, select = "*") {
  const { data, error } = await db.from(table).select(select);
  if (error) throw error;
  return data || [];
}

async function prefetchProject() {
  if (!projectId) return null;
  const { data, error } = await db.from("hospital_projects")
    .select("*,hospital_clients(id,hospital_name,client_code,contact_name,email,phone,whatsapp_phone,billing_email,billing_phone,authorized_representative_name,authorized_representative_email,authorized_representative_phone,authorized_representative_whatsapp,portal_email,portal_phone,billing_address,gstin)")
    .eq("id", projectId).maybeSingle();
  if (error) throw error;
  if (!data) return null;
  const { data: scopeRows, error: scopeError } = await db.from("hospital_project_service_scopes")
    .select("scope_code").eq("project_id", projectId);
  if (scopeError) console.warn("[HOSPITAL_PROJECT_SCOPES_FAILED]", scopeError);
  state.project = data;
  state.scopes = (scopeRows || []).map((row) => row.scope_code);
  return data;
}

async function loadView() {
  const id = projectId;
  if (state.view === "overview") {
    [state.packages, state.items, state.licenses, state.invoices, state.queries, state.procurementPackages] = await Promise.all([
      query("hospital_work_packages").then((rows) => rows.filter((x) => x.project_id === id)),
      query("hospital_procurement_items").then((rows) => rows.filter((x) => x.project_id === id)),
      query("hospital_license_requirements").then((rows) => rows.filter((x) => x.project_id === id)),
      query("hospital_invoice_register").then((rows) => rows.filter((x) => x.project_id === id)),
      query("hospital_queries").then((rows) => rows.filter((x) => x.project_id === id)),
      query("hospital_procurement_package_register").then((rows) => rows.filter((x) => x.project_id === id))
    ]);
  } else if (["planning", "construction"].includes(state.view)) {
    [state.packages, state.vendors] = await Promise.all([
      query("hospital_work_packages", "*,hospital_vendors(id,legal_name,vendor_code)").then((rows) => rows.filter((x) => x.project_id === id)),
      query("hospital_vendors")
    ]);
  } else if (["procurement", "medical-equipment"].includes(state.view)) {
    const [items, vendors, packages, termRows] = await Promise.all([
      query("hospital_procurement_items", "*,hospital_vendors(id,legal_name,vendor_code)").then((rows) => rows.filter((x) => x.project_id === id)),
      query("hospital_vendors"),
      query("hospital_procurement_package_register").then((rows) => rows.filter((x) => x.project_id === id)),
      query("hospital_procurement_packages", "id,project_id,client_terms").then((rows) => rows.filter((x) => x.project_id === id))
    ]);
    const termsByPackage = new Map(termRows.map((row) => [row.id, row.client_terms]));
    state.items = items;
    state.vendors = vendors;
    state.procurementPackages = packages.map((row) => ({ ...row, client_terms: termsByPackage.get(row.id) ?? null }));
    const packageIds = new Set(state.procurementPackages.map((row) => row.id));
    [state.procurementLines, state.procurementCharges, state.procurementTaxes, state.procurementApprovals, state.purchaseOrders] = await Promise.all([
      query("hospital_procurement_package_items", "*,hospital_procurement_items(id,equipment_name,make_model,specification)").then((rows) => rows.filter((x) => packageIds.has(x.package_id))),
      query("hospital_procurement_package_charges").then((rows) => rows.filter((x) => packageIds.has(x.package_id))),
      query("hospital_procurement_package_taxes").then((rows) => rows.filter((x) => packageIds.has(x.package_id))),
      query("hospital_procurement_approval_register").then((rows) => rows.filter((x) => x.project_id === id)),
      query("hospital_purchase_order_register").then((rows) => rows.filter((x) => x.project_id === id))
    ]);
    if (!state.procurementPackages.some((row) => row.id === state.activeProcurementPackageId)) state.activeProcurementPackageId = null;
  } else if (state.view === "licensing") {
    [state.licenses, state.vendors] = await Promise.all([
      query("hospital_license_requirements", "*,hospital_vendors!hospital_license_requirements_responsible_vendor_id_fkey(id,legal_name,vendor_code)").then((rows) => rows.filter((x) => x.project_id === id)),
      query("hospital_vendors")
    ]);
  } else if (state.view === "contractors") {
    [state.vendors, state.assignments, state.packages] = await Promise.all([
      query("hospital_vendors"), query("hospital_project_vendors", "*,hospital_vendors(id,legal_name,vendor_code)").then((rows) => rows.filter((x) => x.project_id === id)),
      query("hospital_work_packages", "*,hospital_vendors(id,legal_name,vendor_code)").then((rows) => rows.filter((x) => x.project_id === id))
    ]);
  } else if (state.view === "documents") {
    state.documents = (await query("drive_documents")).filter((x) => x.category === "HOSPITAL_PROJECT" && x.entity_id === id && !x.deleted_at);
  } else if (state.view === "quotations") {
    [state.quotations, state.quotationLines] = await Promise.all([
      query("hospital_quotation_register").then((rows) => rows.filter((x) => x.project_id === id)),
      query("hospital_quotation_lines")
    ]);
  } else if (state.view === "client-payments") {
    [state.invoices, state.invoiceLines, state.credits, state.documents] = await Promise.all([
      query("hospital_invoice_register").then((rows) => rows.filter((x) => x.project_id === id)),
      query("hospital_invoice_lines"),
      query("hospital_credit_notes", "*,hospital_invoices(invoice_number)").then((rows) => rows.filter((x) => x.project_id === id)),
      query("drive_documents").then((rows)=>rows.filter((x)=>x.category==="HOSPITAL_PROJECT"&&["hospital_invoices","hospital_credit_notes"].includes(x.entity_type)&&!x.deleted_at))
    ]);
  } else if (state.view === "vendor-payments") {
    [state.vendors, state.assignments, state.vendorBills] = await Promise.all([
      query("hospital_vendors"),
      query("hospital_project_vendors", "*,hospital_vendors(id,legal_name,vendor_code)").then((rows) => rows.filter((x) => x.project_id === id && x.status !== "cancelled")),
      query("hospital_vendor_bill_register").then((rows) => rows.filter((x) => x.project_id === id))
    ]);
  } else if (state.view === "communications") {
    const [queries, emailHistory, whatsappHistory] = await Promise.all([query("hospital_queries"), listEmailHistory().catch(() => ({ outbox: [] })), listWhatsAppHistory().catch(() => ({ messages: [], logs: [] }))]);
    state.queries = queries.filter((x) => x.project_id === id);
    const queryIds = new Set(state.queries.map((x) => x.id));
    state.messages = (await query("hospital_query_messages")).filter((x) => queryIds.has(x.query_id));
    state.emails = (emailHistory.outbox || []).filter((x) => x.source_module === "hospital-projects" && x.source_event === state.project.project_code);
    const whatsappMessages=whatsappHistory.messages||[],contactPhone=normalizePhone(projectContact().phone);
    state.whatsapp = whatsappMessages.filter((x) => x.source_module === "hospital-projects" && x.source_event === state.project.project_code);
    state.whatsappContactHistory = whatsappMessages.filter((x) => normalizePhone(x.phone) === contactPhone).sort((a,b)=>new Date(a.created_at)-new Date(b.created_at));
  } else if (state.view === "reports") {
    [state.packages, state.items, state.licenses, state.invoices, state.procurementPackages] = await Promise.all([
      query("hospital_work_packages").then((rows) => rows.filter((x) => x.project_id === id)),
      query("hospital_procurement_items").then((rows) => rows.filter((x) => x.project_id === id)),
      query("hospital_license_requirements").then((rows) => rows.filter((x) => x.project_id === id)),
      query("hospital_invoice_register").then((rows) => rows.filter((x) => x.project_id === id)),
      query("hospital_procurement_package_register").then((rows) => rows.filter((x) => x.project_id === id))
    ]);
  }
}

function table(headers, rows, empty = "No records yet") {
  return `<div class="table-container"><table><thead><tr>${headers.map((x) => `<th>${x}</th>`).join("")}</tr></thead><tbody>${rows || `<tr><td colspan="${headers.length}" class="hp-empty">${esc(empty)}</td></tr>`}</tbody></table></div>`;
}
function kpi(title, value, note) { return `<article class="hp-kpi"><span>${esc(title)}</span><strong>${value}</strong><small>${esc(note)}</small></article>`; }
function projectHeader() { return `<section class="hp-hero"><div><span class="hp-eyebrow">${esc(state.project.project_code)}</span><h2>${esc(state.project.title)}</h2><p>${esc(state.project.hospital_clients?.hospital_name || "Hospital project")} · ${esc(state.project.location || "Location pending")}</p></div><span class="meta-pill">${esc(label(state.project.status))}</span></section>`; }

function overview() {
  const billed = state.invoices.reduce((sum, row) => sum + Number(row.total_amount || 0), 0);
  const outstanding = state.invoices.reduce((sum, row) => sum + Number(row.balance_amount || 0), 0);
  const margin = [...state.packages, ...state.procurementPackages, ...state.licenses].reduce((sum, row) => sum + Number(row.margin_amount || 0), 0);
  const links = workspaceItems().filter((x) => x.view !== "overview").map((x) => `<a class="hp-workspace-link" href="${route(x.view)}"><strong>${esc(x.label)}</strong><small>Open dedicated project page</small></a>`).join("");
  return `${projectHeader()}<div class="hp-kpis">${kpi("Progress", `${Number(state.project.progress_percent || 0)}%`, date(state.project.target_date))}${kpi("Client billing", money(billed), `${money(outstanding)} outstanding`)}${kpi("Planned margin", money(margin), "Internal only")}${kpi("Open queries", state.queries.filter((x) => !["resolved", "closed"].includes(x.status)).length, `${state.queries.length} total`)}</div><section class="hp-card"><h3>Service scopes</h3><div class="hp-actions">${state.scopes.map((x) => `<span class="meta-pill">${esc(SERVICE_SCOPES[x] || label(x))}</span>`).join("")}</div></section><section class="hp-card"><h3>Project workspace</h3><div class="hp-workspace-links">${links}</div></section>`;
}

function packagesPage(kind) {
  const allowed = kind === "planning" ? ["planning_design"] : ["civil_construction", "mep", "healthcare_interiors", "commissioning"];
  const rows = state.packages.filter((x) => allowed.includes(x.service_type));
  const vendorOptions = state.vendors.map((x) => `<option value="${x.id}">${esc(x.vendor_code)} · ${esc(x.legal_name)}</option>`).join("");
  const serviceOptions = allowed.map((x) => `<option value="${x}">${esc(label(x))}</option>`).join("");
  return `${projectHeader()}${can(PERMISSIONS.CREATE) ? `<section class="hp-card"><h3>Add ${esc(kind)} work package</h3><p class="muted">Create and manage this package inside the project workspace. Vendor cost and margin remain internal.</p><form id="projectPackageForm" class="hp-form"><label>Package code<input name="package_code" required placeholder="PKG-01"></label><label>Package title<input name="title" required></label><label>Service<select name="service_type">${serviceOptions}</select></label><label>Delivery model<select name="delivery_model"><option value="subcontract">Subcontract</option><option value="resale">Resale</option><option value="consultancy">Consultancy</option><option value="in_house">In house</option></select></label><label>Vendor<select name="vendor_id"><option value="">Unassigned</option>${vendorOptions}</select></label><label>Vendor cost<input name="vendor_cost" type="number" min="0" step=".01" value="0"></label><label>Additional cost<input name="additional_cost" type="number" min="0" step=".01" value="0"></label><label>Client selling price<input name="client_price" type="number" min="0" step=".01" value="0"></label><label class="wide">Scope summary<textarea name="scope_summary"></textarea></label><button class="btn">Add work package</button></form></section>` : ""}<section class="hp-card"><h3>${esc(PAGE_META[kind][0])}</h3><p class="muted">Project-scoped work package register.</p>${table(["Package", "Service", "Vendor", "Delivery", "Client price", "Margin", "Status"], rows.map((x) => `<tr><td><strong>${esc(x.title)}</strong><br><small>${esc(x.package_code)}</small></td><td>${esc(label(x.service_type))}</td><td>${esc(x.hospital_vendors?.legal_name || "Unassigned")}</td><td>${esc(label(x.delivery_model))}</td><td>${money(x.client_price)}</td><td>${money(x.margin_amount)}</td><td>${esc(label(x.status))}</td></tr>`).join(""), `No ${kind} work packages`)}</section>`;
}
function equipmentPage(title) {
  const editing = state.items.find((x) => x.id === state.equipmentEditingId) || null;
  const vendorSelect = `<option value="">Unassigned</option>${state.vendors.map((x) => `<option value="${x.id}" ${editing?.vendor_id === x.id ? "selected" : ""}>${esc(x.vendor_code)} · ${esc(x.legal_name)}</option>`).join("")}`;
  const statusSelect = ["requirement","rfq","vendor_selected","client_quoted","ordered","in_transit","delivered","installed","commissioned","cancelled"].map((x) => `<option value="${x}" ${(editing?.status || "requirement") === x ? "selected" : ""}>${esc(label(x))}</option>`).join("");
  const canSave = editing ? can(PERMISSIONS.EDIT) : can(PERMISSIONS.CREATE);
  const actions = can(PERMISSIONS.EDIT) || can(PERMISSIONS.DELETE);
  const active = state.procurementPackages.find((x) => x.id === state.activeProcurementPackageId) || null;
  const packageVendorOptions = state.vendors.filter((x) => x.status === "active").map((x) => `<option value="${x.id}">${esc(x.vendor_code)} · ${esc(x.legal_name)}</option>`).join("");
  const requirementChoices = state.items.filter((x) => x.status !== "cancelled").map((x) => `<label class="hp-scope-option"><input type="checkbox" name="procurement_item_ids" value="${x.id}"><span><strong>${esc(x.equipment_name)}</strong><small>${Number(x.quantity)} ${esc(x.unit || "Nos")} · ${esc(x.make_model || "No model")}</small></span></label>`).join("");
  const packageRows = state.procurementPackages.map((x) => `<tr><td><strong>${esc(x.package_number)}</strong><br><small>${esc(x.vendor_quote_number || "No quote reference")}</small></td><td>${esc(x.vendor_name)}</td><td>${x.item_count}</td><td>${money(x.vendor_package_total)}</td><td>${money(x.client_package_total)}</td><td>${money(x.margin_amount)}</td><td>${esc(label(x.tax_mode))}<br><small>${x.tax_mode === "common" ? `${Number(x.common_gst_rate)}% GST` : "Per line GST"}</small></td><td>${esc(label(x.status))}</td><td><div class="hp-actions"><button class="btn btn-sm" type="button" data-open-procurement-package="${x.id}">Manage</button>${can(PERMISSIONS.DELETE) && x.status === "draft" ? `<button class="btn btn-sm" type="button" data-delete-procurement-package="${x.id}">Delete</button>` : ""}</div></td></tr>`).join("");
  const activeLines = active ? state.procurementLines.filter((x) => x.package_id === active.id) : [];
  const activeCharges = active ? state.procurementCharges.filter((x) => x.package_id === active.id) : [];
  const activeTaxes = active ? state.procurementTaxes.filter((x) => x.package_id === active.id) : [];
  const activeLineItemIds = new Set(activeLines.map((x) => x.procurement_item_id));
  const additionalRequirementOptions = active ? state.items.filter((x) => x.status !== "cancelled" && !activeLineItemIds.has(x.id)).map((x) => `<option value="${x.id}" data-quantity="${Number(x.quantity)}" data-unit="${esc(x.unit || "Nos")}">${esc(x.equipment_name)} · ${Number(x.quantity)} ${esc(x.unit || "Nos")}</option>`).join("") : "";
  const packageStatusOptions = active ? ["draft","rfq","quote_received","approved","purchase_order","delivery","installed","vendor_billed","completed","cancelled"].map((x) => `<option value="${x}" ${active.status === x ? "selected" : ""}>${esc(label(x))}</option>`).join("") : "";
  const pricingWorkspace = active ? `<section class="hp-card"><div class="hp-actions" style="justify-content:space-between"><div><h3>Package pricing · ${esc(active.package_number)}</h3><p class="muted">${esc(active.vendor_name)} · Package charges remain separate from item prices.</p></div><button class="btn btn-sm" id="closeProcurementPackage" type="button">Close</button></div>
    ${can(PERMISSIONS.EDIT) ? `<form id="procurementPackageHeaderForm" class="hp-form"><label>Quote reference<input name="vendor_quote_number" value="${esc(active.vendor_quote_number || "")}"></label><label>Quote date<input name="quote_date" type="date" value="${esc(active.quote_date || "")}"></label><label>Valid until<input name="valid_until" type="date" value="${esc(active.valid_until || "")}"></label><label>Expected delivery<input name="expected_delivery" type="date" value="${esc(active.expected_delivery || "")}"></label><label>Place of supply<input name="place_of_supply" value="${esc(active.place_of_supply || "")}"></label><label>GST method<select name="tax_mode"><option value="common" ${active.tax_mode === "common" ? "selected" : ""}>Common bill GST</option><option value="item_wise" ${active.tax_mode === "item_wise" ? "selected" : ""}>Item / charge-wise GST</option></select></label><label>Common GST %<input name="common_gst_rate" type="number" min="0" max="100" step=".001" value="${Number(active.common_gst_rate || 0)}"></label><label>Status<select name="status">${packageStatusOptions}</select></label><label>Vendor round off<input name="vendor_round_off" type="number" step=".01" value="${Number(active.vendor_round_off || 0)}"></label><label>Client round off<input name="client_round_off" type="number" step=".01" value="${Number(active.client_round_off || 0)}"></label><label class="wide">Notes<textarea name="notes">${esc(active.notes || "")}</textarea></label><button class="btn">Save package</button></form>` : ""}
    <h3>Item pricing</h3><p class="muted">Discount is the total discount for that line. GST is used only when the package method is item-wise.</p>${table(["Requirement","Qty / unit","Vendor unit","Vendor discount","Client unit","Client discount","HSN/SAC","GST %","Actions"], activeLines.map((line) => `<tr data-procurement-line="${line.id}"><td><strong>${esc(line.hospital_procurement_items?.equipment_name || "Requirement")}</strong><br><small>${esc(line.hospital_procurement_items?.make_model || "")}</small></td><td><input name="quantity" type="number" min=".001" step=".001" value="${Number(line.quantity)}"><input name="unit" value="${esc(line.unit)}"></td><td><input name="vendor_unit_price" type="number" min="0" step=".01" value="${Number(line.vendor_unit_price)}"></td><td><input name="vendor_discount" type="number" min="0" step=".01" value="${Number(line.vendor_discount)}"></td><td><input name="client_unit_price" type="number" min="0" step=".01" value="${Number(line.client_unit_price)}"></td><td><input name="client_discount" type="number" min="0" step=".01" value="${Number(line.client_discount)}"></td><td><input name="hsn_sac" value="${esc(line.hsn_sac || "")}"></td><td><input name="gst_rate" type="number" min="0" max="100" step=".001" value="${Number(line.gst_rate)}" ${active.tax_mode === "common" ? "disabled" : ""}></td><td><div class="hp-actions">${can(PERMISSIONS.EDIT) ? `<button class="btn btn-sm" type="button" data-save-procurement-line="${line.id}">Save</button>` : ""}${can(PERMISSIONS.DELETE) ? `<button class="btn btn-sm" type="button" data-delete-procurement-line="${line.id}">Remove</button>` : ""}</div></td></tr>`).join(""), "No package items")}${can(PERMISSIONS.CREATE) && additionalRequirementOptions ? `<form id="addRequirementToPackageForm" class="hp-form"><label>Add another requirement<select name="procurement_item_id" required><option value="">Select requirement</option>${additionalRequirementOptions}</select></label><button class="btn">Add to package</button></form>` : ""}
    <div class="hp-kpis">${kpi("Vendor base", money(Number(active.vendor_items_subtotal)+Number(active.vendor_charges_subtotal)), `${money(active.vendor_gst)} GST`)}${kpi("Vendor total", money(active.vendor_package_total), `${money(active.vendor_tax_deductions)} deductions`)}${kpi("Client total", money(active.client_package_total), `${money(active.client_gst)} GST`)}${kpi("Margin before tax", money(active.margin_amount), "Recoverable taxes excluded")}</div>
    ${can(PERMISSIONS.CREATE) ? `<h3>Package-level charges</h3><p class="muted">Add transport, installation, packing, insurance or another overall charge once for the package.</p><form id="procurementChargeForm" class="hp-form"><label>Charge<select name="charge_type"><option value="transport">Transport</option><option value="installation">Installation</option><option value="packing">Packing</option><option value="insurance">Insurance</option><option value="other">Other</option></select></label><label>Description<input name="description" required></label><label>Vendor amount<input name="vendor_amount" type="number" min="0" step=".01" value="0"></label><label>Client amount<input name="client_amount" type="number" min="0" step=".01" value="0"></label><label>HSN/SAC<input name="hsn_sac"></label><label>GST %<input name="gst_rate" type="number" min="0" max="100" step=".001" value="0" ${active.tax_mode === "common" ? "disabled" : ""}></label><button class="btn">Add package charge</button></form>` : ""}
    ${table(["Charge","Vendor amount","Client amount","GST","Actions"], activeCharges.map((x) => `<tr><td><strong>${esc(label(x.charge_type))}</strong><br><small>${esc(x.description)}</small></td><td>${money(x.vendor_amount)}</td><td>${money(x.client_amount)}</td><td>${active.tax_mode === "common" ? "Common" : `${Number(x.gst_rate)}%`}</td><td>${can(PERMISSIONS.DELETE) ? `<button class="btn btn-sm" type="button" data-delete-procurement-charge="${x.id}">Delete</button>` : "—"}</td></tr>`).join(""), "No package-level charges")}
    ${can(PERMISSIONS.CREATE) ? `<h3>Other taxes and deductions</h3><p class="muted">Use additive for cess/customs or other taxes, and deduction for TDS or similar deductions. GST is calculated separately above.</p><form id="procurementTaxForm" class="hp-form"><label>Tax / deduction<input name="tax_name" required placeholder="Cess, customs, TDS…"></label><label>Applies to<select name="applies_to"><option value="vendor">Vendor</option><option value="client">Client</option><option value="both">Both</option></select></label><label>Effect<select name="effect"><option value="additive">Additive tax</option><option value="deduction">Deduction</option></select></label><label>Taxable base<input name="taxable_base" type="number" min="0" step=".01" value="0"></label><label>Rate %<input name="tax_rate" type="number" min="0" max="100" step=".001" value="0"></label><label>Amount<input name="tax_amount" type="number" min="0" step=".01" value="0" required></label><label class="wide">Notes<input name="notes"></label><button class="btn">Add tax / deduction</button></form>` : ""}
    ${table(["Tax / deduction","Side","Base / rate","Amount","Actions"], activeTaxes.map((x) => `<tr><td><strong>${esc(x.tax_name)}</strong><br><small>${esc(label(x.effect))}</small></td><td>${esc(label(x.applies_to))}</td><td>${money(x.taxable_base)} · ${Number(x.tax_rate)}%</td><td>${money(x.tax_amount)}</td><td>${can(PERMISSIONS.DELETE) ? `<button class="btn btn-sm" type="button" data-delete-procurement-tax="${x.id}">Delete</button>` : "—"}</td></tr>`).join(""), "No other taxes or deductions")}
  </section>` : "";
  return `${projectHeader()}${canSave ? `<section class="hp-card"><h3>${editing ? "Edit" : "Add"} ${esc(title)} requirement</h3><p class="muted">Capture what the project needs first. Pricing, GST, transport and installation are added later inside a vendor package.</p><form id="projectEquipmentForm" class="hp-form"><label>Equipment / item<input name="equipment_name" value="${esc(editing?.equipment_name || "")}" required></label><label>Make / model<input name="make_model" value="${esc(editing?.make_model || "")}"></label><label>Preferred vendor<select name="vendor_id">${vendorSelect}</select></label><label>Quantity<input name="quantity" type="number" min=".001" step=".001" value="${Number(editing?.quantity || 1)}" required></label><label>Unit<input name="unit" value="${esc(editing?.unit || "Nos")}" required></label><label>Required by<input name="expected_delivery" type="date" value="${esc(editing?.expected_delivery || "")}"></label>${editing ? `<label>Status<select name="status">${statusSelect}</select></label>` : ""}<label class="wide">Specification<textarea name="specification">${esc(editing?.specification || "")}</textarea></label><div class="hp-actions"><button class="btn">${editing ? "Update requirement" : "Add requirement"}</button>${editing ? `<button class="btn" id="cancelEquipmentEdit" type="button">Cancel edit</button>` : ""}</div></form></section>` : ""}
  <section class="hp-card"><h3>${esc(title)} requirements</h3><p class="muted">This register contains quantities and specifications only. A requirement can be included in more than one vendor package.</p>${table(["Requirement","Preferred vendor","Quantity","Required by","Packages","Status",...(actions ? ["Actions"] : [])], state.items.map((x) => { const count=state.procurementLines.filter((line)=>line.procurement_item_id===x.id).length;return `<tr><td><strong>${esc(x.equipment_name)}</strong><br><small>${esc(x.make_model || x.specification || "")}</small></td><td>${esc(x.hospital_vendors?.legal_name || "Unassigned")}</td><td>${Number(x.quantity)} ${esc(x.unit || "Nos")}</td><td>${date(x.expected_delivery)}</td><td>${count || "Not packaged"}</td><td>${esc(label(x.status))}</td>${actions ? `<td><div class="hp-actions">${can(PERMISSIONS.EDIT) ? `<button class="btn btn-sm" type="button" data-edit-equipment="${x.id}">Edit</button>` : ""}${can(PERMISSIONS.DELETE) ? `<button class="btn btn-sm" type="button" data-delete-equipment="${x.id}">Delete</button>` : ""}</div></td>` : ""}</tr>`;}).join(""), "No procurement requirements")}</section>
  ${can(PERMISSIONS.CREATE) ? `<section class="hp-card"><h3>Create vendor procurement package</h3><p class="muted">Group multiple requirements from one vendor quote. Overall transport and installation are added after the package is created.</p><form id="procurementPackageForm" class="hp-form"><label>Vendor<select name="vendor_id" required><option value="">Select active vendor</option>${packageVendorOptions}</select></label><label>Vendor quote reference<input name="vendor_quote_number"></label><label>Quote date<input name="quote_date" type="date"></label><label>Valid until<input name="valid_until" type="date"></label><label>Expected delivery<input name="expected_delivery" type="date"></label><label>Place of supply<input name="place_of_supply"></label><label>GST method<select name="tax_mode"><option value="common">Common bill GST</option><option value="item_wise">Item / charge-wise GST</option></select></label><label>Common GST %<input name="common_gst_rate" type="number" min="0" max="100" step=".001" value="18"></label><fieldset class="wide"><legend>Select requirements</legend><div class="hp-scope-options">${requirementChoices || `<p class="muted">Add requirements first.</p>`}</div></fieldset><label class="wide">Notes<textarea name="notes"></textarea></label><button class="btn" ${requirementChoices ? "" : "disabled"}>Create package</button></form></section>` : ""}
  <section class="hp-card"><h3>Vendor procurement packages</h3><p class="muted">One vendor may have multiple packages in this project. Totals include package charges and tax rules without allocating those charges across items.</p>${table(["Package","Vendor","Items","Vendor total","Client total","Margin","Tax","Status","Actions"],packageRows,"No procurement packages")}</section>${pricingWorkspace}`;
}
const PROCUREMENT_TERM_FIELDS = [
  ["payment_terms", "Payment terms"],
  ["delivery_timeline", "Delivery timeline"],
  ["installation_terms", "Installation & commissioning"],
  ["warranty_terms", "Warranty & support"],
  ["additional_terms", "Additional terms"]
];

function composeProcurementTerms(payload = {}) {
  return PROCUREMENT_TERM_FIELDS.map(([key, title]) => {
    const detail = String(payload[key] || "").trim();
    return detail ? `${title}: ${detail}` : "";
  }).filter(Boolean).join("\n");
}

function parseProcurementTerms(terms = "") {
  const result = Object.fromEntries(PROCUREMENT_TERM_FIELDS.map(([key]) => [key, ""]));
  const labelToKey = new Map(PROCUREMENT_TERM_FIELDS.map(([key, title]) => [title.toLowerCase(), key]));
  for (const rawLine of String(terms || "").split("\n")) {
    const line = rawLine.trim();
    if (!line) continue;
    const separator = line.indexOf(":");
    const key = separator > 0 ? labelToKey.get(line.slice(0, separator).trim().toLowerCase()) : null;
    if (key) result[key] = line.slice(separator + 1).trim();
    else result.additional_terms = [result.additional_terms, line].filter(Boolean).join("\n");
  }
  return result;
}

function procurementTermsFor(active, latestApproval = null) {
  if (!active) return "";
  if (Object.prototype.hasOwnProperty.call(state.procurementTermsByPackage, active.id)) return state.procurementTermsByPackage[active.id];
  const saved = Object.prototype.hasOwnProperty.call(active, "client_terms")
    ? active.client_terms || ""
    : latestApproval?.payload_snapshot?.terms || latestApproval?.terms || "";
  state.procurementTermsByPackage[active.id] = saved;
  return saved;
}

function procurementTermsMarkup(terms) {
  const rows = String(terms || "").split("\n").map((line) => line.trim()).filter(Boolean);
  return `<div class="hp-terms-fine-print"><strong>Terms & Conditions</strong>${rows.length ? `<ol>${rows.map((line) => `<li>${esc(line)}</li>`).join("")}</ol>` : `<p>No additional commercial terms entered.</p>`}</div>`;
}

function procurementClientSnapshot(active, terms = "") {
  const client = state.project.hospital_clients || {};
  const lines = state.procurementLines.filter((row) => row.package_id === active.id);
  const charges = state.procurementCharges.filter((row) => row.package_id === active.id);
  const taxes = state.procurementTaxes.filter((row) => row.package_id === active.id && ["client", "both"].includes(row.applies_to));
  return {
    approvalNumber: `DRAFT · ${active.package_number}`, revisionNo: 1, generatedAt: new Date().toISOString(),
    project: { id: state.project.id, code: state.project.project_code, title: state.project.title, location: state.project.location },
    client: { id: client.id, code: client.client_code, name: client.hospital_name, billingAddress: client.billing_address, gstin: client.gstin },
    package: { id: active.id, number: active.package_number, validUntil: active.valid_until, expectedDelivery: active.expected_delivery, placeOfSupply: active.place_of_supply, taxMode: active.tax_mode, commonGstRate: active.common_gst_rate, notes: active.notes },
    items: lines.map((line) => { const gstRate=Number(active.tax_mode === "common" ? active.common_gst_rate : line.gst_rate);const entered=Number(line.quantity)*Number(line.client_unit_price)-Number(line.client_discount);return { name: line.hospital_procurement_items?.equipment_name, makeModel: line.hospital_procurement_items?.make_model, specification: line.hospital_procurement_items?.specification, quantity: line.quantity, unit: line.unit, unitPrice: line.client_unit_price, discount: line.client_discount, hsnSac: line.hsn_sac, gstRate, gstIncluded:Boolean(line.gst_included), taxableAmount:line.gst_included&&gstRate>0?entered*100/(100+gstRate):entered }; }),
    charges: charges.filter((row) => Number(row.client_amount) > 0).map((row) => { const gstRate=Number(active.tax_mode === "common" ? active.common_gst_rate : row.gst_rate),entered=Number(row.client_amount);return { type: row.charge_type, description: row.description, amount: row.client_amount, hsnSac: row.hsn_sac, gstRate, gstIncluded:Boolean(row.gst_included), taxableAmount:row.gst_included&&gstRate>0?entered*100/(100+gstRate):entered }; }),
    otherTaxes: taxes.map((row) => ({ name: row.tax_name, effect: row.effect, taxableBase: row.taxable_base, rate: row.tax_rate, amount: row.tax_amount, notes: row.notes })),
    totals: { itemsSubtotal: active.client_items_subtotal, chargesSubtotal: active.client_charges_subtotal, gst: active.client_gst, additions: active.client_tax_additions, deductions: active.client_tax_deductions, roundOff: active.client_round_off, grandTotal: active.client_package_total },
    terms: String(terms || "").trim() || null
  };
}

function procurementWizardControls({ next = true, skip = false } = {}) {
  return `<div class="hp-actions hp-wizard-actions">${state.procurementWizardStep > 1 ? `<button class="btn" type="button" data-procurement-back>Back</button>` : ""}<span class="hp-wizard-spacer"></span>${skip ? `<button class="btn" type="button" data-procurement-skip>Skip for now</button>` : ""}${next ? `<button class="btn" type="button" data-procurement-next>Next</button>` : ""}</div>`;
}

function procurementChargeForm(active, editing) {
  const chargeTypes=["transport","installation","packing","insurance","other"].map((type)=>`<option value="${type}" ${(editing?.charge_type||"transport")===type?"selected":""}>${esc(label(type))}</option>`).join("");
  return `<h3>${editing?"Edit":"Add"} package-level charge</h3><p class="muted">Enter the quoted amount and choose whether it already includes GST.</p><form id="procurementChargeForm" class="hp-form"><label>Charge<select name="charge_type">${chargeTypes}</select></label><label>Description<input name="description" value="${esc(editing?.description||"")}" required></label><label>Vendor amount<input name="vendor_amount" type="number" min="0" step=".01" value="${Number(editing?.vendor_amount||0)}"></label><label>Client amount<input name="client_amount" type="number" min="0" step=".01" value="${Number(editing?.client_amount||0)}"></label><label>HSN/SAC<input name="hsn_sac" value="${esc(editing?.hsn_sac||"")}"></label><label>GST %<input name="gst_rate" type="number" min="0" max="100" step=".001" value="${Number(editing?.gst_rate||0)}" ${active.tax_mode==="common"?"disabled":""}></label><label>GST treatment<select name="gst_included"><option value="false" ${editing?.gst_included?"":"selected"}>GST excluded (add GST)</option><option value="true" ${editing?.gst_included?"selected":""}>GST included (extract GST)</option></select></label><div class="wide hp-actions"><button class="btn">${editing?"Update charge":"Add charge"}</button>${editing?`<button class="btn" id="cancelProcurementChargeEdit" type="button">Cancel edit</button>`:""}</div></form>`;
}

function procurementWizardPage(title) {
  const step = state.procurementWizardStep;
  const editing = state.items.find((row) => row.id === state.equipmentEditingId) || null;
  const active = state.procurementPackages.find((row) => row.id === state.activeProcurementPackageId) || null;
  const approvals = active ? state.procurementApprovals.filter((row) => row.package_id === active.id).sort((a, b) => Number(b.revision_no) - Number(a.revision_no)) : [];
  const latestApproval = approvals[0] || null;
  const purchaseOrder = active ? state.purchaseOrders.find((row) => row.package_id === active.id) || null : null;
  const locked = ["pending", "approved"].includes(latestApproval?.status);
  const steps = ["Requirements", "Vendor package", "Item pricing", "Charges & taxes", "Terms & conditions", "Client review"];
  const stepper = `<style>.hp-wizard-steps{display:grid;grid-template-columns:repeat(6,minmax(0,1fr));gap:.5rem;margin:0 0 1rem}.hp-wizard-step{border:1px solid rgba(196,148,63,.35);background:#0c1015;color:#aaa;padding:.7rem;border-radius:12px;text-align:left}.hp-wizard-step.active{border-color:#d9b95f;color:#fff;background:rgba(196,148,63,.13)}.hp-wizard-step.done{color:#d9b95f}.hp-wizard-step small{display:block;margin-bottom:.2rem}.hp-wizard-actions{margin-top:1rem}.hp-wizard-spacer{flex:1}.hp-review-total{font-size:1.35rem}.hp-review-note{border-left:3px solid #d9b95f;padding:.8rem 1rem;background:rgba(196,148,63,.07)}.hp-terms-fine-print{margin:1rem 0;padding:1rem 1.1rem;border:1px solid rgba(196,148,63,.35);border-radius:12px;background:rgba(196,148,63,.055);font-size:.78rem;line-height:1.55;color:#c9c1b2}.hp-terms-fine-print>strong{display:block;color:#e1c16b;text-transform:uppercase;letter-spacing:.08em;margin-bottom:.45rem}.hp-terms-fine-print ol{margin:.25rem 0 0;padding-left:1.2rem}.hp-terms-fine-print li+li{margin-top:.3rem}@media(max-width:1000px){.hp-wizard-steps{grid-template-columns:repeat(3,1fr)}}@media(max-width:800px){.hp-wizard-steps{grid-template-columns:1fr 1fr}}</style><div class="hp-wizard-steps">${steps.map((name, index) => `<button type="button" class="hp-wizard-step ${step === index + 1 ? "active" : step > index + 1 ? "done" : ""}" data-procurement-step="${index + 1}"><small>STEP ${index + 1}</small>${esc(name)}</button>`).join("")}</div>`;
  let content = "";

  if (step === 1) {
    const vendorOptions = `<option value="">Unassigned</option>${state.vendors.map((row) => `<option value="${row.id}" ${editing?.vendor_id === row.id ? "selected" : ""}>${esc(row.vendor_code)} · ${esc(row.legal_name)}</option>`).join("")}`;
    const statusOptions = ["requirement","rfq","vendor_selected","client_quoted","ordered","in_transit","delivered","installed","commissioned","cancelled"].map((status) => `<option value="${status}" ${(editing?.status || "requirement") === status ? "selected" : ""}>${esc(label(status))}</option>`).join("");
    content = `${(editing ? can(PERMISSIONS.EDIT) : can(PERMISSIONS.CREATE)) ? `<section class="hp-card"><h3>${editing ? "Edit" : "Add"} ${esc(title)} requirement</h3><p class="muted">List everything the project needs. Commercial pricing is completed in later steps.</p><form id="projectEquipmentForm" class="hp-form"><label>Equipment / item<input name="equipment_name" value="${esc(editing?.equipment_name || "")}" required></label><label>Make / model<input name="make_model" value="${esc(editing?.make_model || "")}"></label><label>Preferred vendor<select name="vendor_id">${vendorOptions}</select></label><label>Quantity<input name="quantity" type="number" min=".001" step=".001" value="${Number(editing?.quantity || 1)}" required></label><label>Unit<input name="unit" value="${esc(editing?.unit || "Nos")}" required></label><label>Required by<input name="expected_delivery" type="date" value="${esc(editing?.expected_delivery || "")}"></label>${editing ? `<label>Status<select name="status">${statusOptions}</select></label>` : ""}<label class="wide">Specification<textarea name="specification">${esc(editing?.specification || "")}</textarea></label><div class="hp-actions"><button class="btn">${editing ? "Update requirement" : "Add requirement"}</button>${editing ? `<button class="btn" id="cancelEquipmentEdit" type="button">Cancel edit</button>` : ""}</div></form></section>` : ""}<section class="hp-card"><h3>Requirement register</h3>${table(["Requirement","Preferred vendor","Quantity","Required by","Status","Actions"], state.items.map((row) => `<tr><td><strong>${esc(row.equipment_name)}</strong><br><small>${esc(row.make_model || row.specification || "")}</small></td><td>${esc(row.hospital_vendors?.legal_name || "Unassigned")}</td><td>${Number(row.quantity)} ${esc(row.unit || "Nos")}</td><td>${date(row.expected_delivery)}</td><td>${esc(label(row.status))}</td><td><div class="hp-actions">${can(PERMISSIONS.EDIT) ? `<button class="btn btn-sm" type="button" data-edit-equipment="${row.id}">Edit</button>` : ""}${can(PERMISSIONS.DELETE) ? `<button class="btn btn-sm" type="button" data-delete-equipment="${row.id}">Delete</button>` : ""}</div></td></tr>`).join(""), "No procurement requirements")}${procurementWizardControls({ next: state.items.length > 0 })}</section>`;
  }

  if (step === 2) {
    const packageEditing = state.procurementPackages.find((row) => row.id === state.procurementPackageEditingId) || null;
    const vendorOptions = state.vendors.filter((row) => row.status === "active").map((row) => `<option value="${row.id}">${esc(row.vendor_code)} · ${esc(row.legal_name)}</option>`).join("");
    const requirementChoices = state.items.filter((row) => row.status !== "cancelled").map((row) => `<label class="hp-scope-option"><input type="checkbox" name="procurement_item_ids" value="${row.id}"><span><strong>${esc(row.equipment_name)}</strong><small>${Number(row.quantity)} ${esc(row.unit || "Nos")} · ${esc(row.make_model || "No model")}</small></span></label>`).join("");
    const createForm = !packageEditing && can(PERMISSIONS.CREATE) ? `<section class="hp-card"><h3>Create vendor procurement package</h3><p class="muted">Group the selected requirements under one vendor quote.</p><form id="procurementPackageForm" class="hp-form"><label>Vendor<select name="vendor_id" required><option value="">Select active vendor</option>${vendorOptions}</select></label><label>Vendor quote reference<input name="vendor_quote_number"></label><label>Quote date<input name="quote_date" type="date"></label><label>Valid until<input name="valid_until" type="date"></label><label>Expected delivery<input name="expected_delivery" type="date"></label><label>Place of supply<input name="place_of_supply"></label><label>GST method<select name="tax_mode"><option value="common">Common bill GST</option><option value="item_wise">Item / charge-wise GST</option></select></label><label>Common GST %<input name="common_gst_rate" type="number" min="0" max="100" step=".001" value="18"></label><fieldset class="wide"><legend>Select requirements</legend><div class="hp-scope-options">${requirementChoices}</div></fieldset><label class="wide">Notes<textarea name="notes"></textarea></label><button class="btn">Create package</button></form></section>` : "";
    const editForm = packageEditing && can(PERMISSIONS.EDIT) ? `<section class="hp-card"><h3>Edit vendor package · ${esc(packageEditing.package_number)}</h3><p class="muted">Vendor: ${esc(packageEditing.vendor_name)}. The vendor identity and package number remain fixed.</p><form id="procurementPackageHeaderForm" class="hp-form"><label>Vendor quote reference<input name="vendor_quote_number" value="${esc(packageEditing.vendor_quote_number || "")}"></label><label>Quote date<input name="quote_date" type="date" value="${esc(packageEditing.quote_date || "")}"></label><label>Valid until<input name="valid_until" type="date" value="${esc(packageEditing.valid_until || "")}"></label><label>Expected delivery<input name="expected_delivery" type="date" value="${esc(packageEditing.expected_delivery || "")}"></label><label>Place of supply<input name="place_of_supply" value="${esc(packageEditing.place_of_supply || "")}"></label><label>GST method<select name="tax_mode"><option value="common" ${packageEditing.tax_mode==="common"?"selected":""}>Common package GST</option><option value="item_wise" ${packageEditing.tax_mode==="item_wise"?"selected":""}>Item / charge-wise GST</option></select></label><label>Common GST %<input name="common_gst_rate" type="number" min="0" max="100" step=".001" value="${Number(packageEditing.common_gst_rate || 0)}"></label><label>Vendor round off<input name="vendor_round_off" type="number" step=".01" value="${Number(packageEditing.vendor_round_off || 0)}"></label><label>Client round off<input name="client_round_off" type="number" step=".01" value="${Number(packageEditing.client_round_off || 0)}"></label><label class="wide">Notes<textarea name="notes">${esc(packageEditing.notes || "")}</textarea></label><div class="wide hp-actions"><button class="btn">Save package</button><button class="btn" id="cancelProcurementPackageEdit" type="button">Cancel</button></div></form></section>` : "";
    const packageRows = state.procurementPackages.map((row) => { const hasApproval=state.procurementApprovals.some((approval)=>approval.package_id===row.id);return `<tr><td><strong>${esc(row.package_number)}</strong><br><small>${esc(row.vendor_quote_number || "No quote reference")}</small></td><td>${esc(row.vendor_name)}</td><td>${row.item_count}</td><td>${money(row.vendor_package_total)}</td><td>${money(row.client_package_total)}</td><td>${esc(label(row.status))}</td><td><div class="hp-actions"><button class="btn btn-sm" type="button" data-open-procurement-package="${row.id}">Select & continue</button>${can(PERMISSIONS.EDIT)?`<button class="btn btn-sm" type="button" data-edit-procurement-package="${row.id}">Edit</button>`:""}${can(PERMISSIONS.DELETE)&&row.status==="draft"&&!hasApproval?`<button class="btn btn-sm" type="button" data-delete-procurement-package="${row.id}">Delete</button>`:""}</div></td></tr>`; }).join("");
    content = `${editForm}${createForm}<section class="hp-card"><h3>Vendor packages</h3>${table(["Package","Vendor","Items","Vendor total","Client total","Status","Actions"],packageRows,"No procurement packages")}${procurementWizardControls({ next: Boolean(active) })}</section>`;
  }

  if (step === 3 && active) {
    const lines = state.procurementLines.filter((row) => row.package_id === active.id);
    const included = new Set(lines.map((row) => row.procurement_item_id));
    const additional = state.items.filter((row) => row.status !== "cancelled" && !included.has(row.id)).map((row) => `<option value="${row.id}" data-quantity="${Number(row.quantity)}" data-unit="${esc(row.unit || "Nos")}">${esc(row.equipment_name)} · ${Number(row.quantity)} ${esc(row.unit || "Nos")}</option>`).join("");
    content = `<section class="hp-card"><h3>Item pricing · ${esc(active.package_number)}</h3><p class="muted">${esc(active.vendor_name)}. Prices are before GST by default. Select “GST included” only when the entered item price already contains GST.</p>${locked ? `<p class="hp-review-note">Pricing is locked while revision ${latestApproval.revision_no} is ${esc(label(latestApproval.status))}.</p>` : ""}${table(["Requirement","Qty / unit","Vendor unit","Vendor discount","Client unit","Client discount","HSN/SAC","GST treatment","Actions"], lines.map((line) => `<tr data-procurement-line="${line.id}"><td><strong>${esc(line.hospital_procurement_items?.equipment_name || "Requirement")}</strong><br><small>${esc(line.hospital_procurement_items?.make_model || "")}</small></td><td><input name="quantity" type="number" min=".001" step=".001" value="${Number(line.quantity)}"><input name="unit" value="${esc(line.unit)}"></td><td><input name="vendor_unit_price" type="number" min="0" step=".01" value="${Number(line.vendor_unit_price)}"></td><td><input name="vendor_discount" type="number" min="0" step=".01" value="${Number(line.vendor_discount)}"></td><td><input name="client_unit_price" type="number" min="0" step=".01" value="${Number(line.client_unit_price)}"></td><td><input name="client_discount" type="number" min="0" step=".01" value="${Number(line.client_discount)}"></td><td><input name="hsn_sac" value="${esc(line.hsn_sac || "")}"></td><td><input name="gst_rate" type="number" min="0" max="100" step=".001" value="${Number(line.gst_rate)}" ${active.tax_mode === "common" ? "disabled" : ""}><select name="gst_included"><option value="false" ${line.gst_included ? "" : "selected"}>GST excluded (add GST)</option><option value="true" ${line.gst_included ? "selected" : ""}>GST included (extract GST)</option></select></td><td>${!locked && can(PERMISSIONS.EDIT) ? `<button class="btn btn-sm" type="button" data-save-procurement-line="${line.id}">Save</button>` : "Locked"}</td></tr>`).join(""), "No package items")}${!locked && can(PERMISSIONS.CREATE) && additional ? `<form id="addRequirementToPackageForm" class="hp-form"><label>Add requirement<select name="procurement_item_id" required><option value="">Select</option>${additional}</select></label><button class="btn">Add to package</button></form>` : ""}${procurementWizardControls()}</section>`;
  }

  if (step === 4 && active) {
    const charges = state.procurementCharges.filter((row) => row.package_id === active.id);
    const taxes = state.procurementTaxes.filter((row) => row.package_id === active.id);
    const editingCharge=charges.find((row)=>row.id===state.procurementChargeEditingId)||null;
    const chargeRows=charges.map((row)=>`<tr><td><strong>${esc(label(row.charge_type))}</strong><br><small>${esc(row.description)}</small></td><td>${money(row.vendor_amount)}</td><td>${money(row.client_amount)}</td><td>${active.tax_mode==="common"?`${Number(active.common_gst_rate)}%`:`${Number(row.gst_rate)}%`}<br><small>${row.gst_included?"Included":"Excluded"}</small></td><td><div class="hp-actions">${can(PERMISSIONS.EDIT)?`<button class="btn btn-sm" type="button" data-edit-procurement-charge="${row.id}">Edit</button>`:""}${can(PERMISSIONS.DELETE)?`<button class="btn btn-sm" type="button" data-delete-procurement-charge="${row.id}">Delete</button>`:""}</div></td></tr>`).join("");
    content = `<section class="hp-card"><h3>Charges & taxes · ${esc(active.package_number)}</h3><div class="hp-kpis">${kpi("Vendor total", money(active.vendor_package_total), `${money(active.vendor_gst)} GST`)}${kpi("Client total", money(active.client_package_total), `${money(active.client_gst)} GST`)}${kpi("Planned margin", money(active.margin_amount), "Internal only")}</div>${locked?`<p class="hp-review-note">Editing or deleting a charge will preserve this ${esc(label(latestApproval.status))} revision as history and start a new draft revision.</p>`:""}${!locked&&(editingCharge?can(PERMISSIONS.EDIT):can(PERMISSIONS.CREATE))?procurementChargeForm(active,editingCharge):""}${table(["Charge","Vendor amount","Client amount","GST treatment","Actions"],chargeRows,"No package-level charges")}${!locked&&can(PERMISSIONS.CREATE)?`<h3>Other taxes & deductions</h3><form id="procurementTaxForm" class="hp-form"><label>Tax / deduction<input name="tax_name" required></label><label>Applies to<select name="applies_to"><option value="vendor">Vendor</option><option value="client">Client</option><option value="both">Both</option></select></label><label>Effect<select name="effect"><option value="additive">Additive tax</option><option value="deduction">Deduction</option></select></label><label>Taxable base<input name="taxable_base" type="number" min="0" step=".01" value="0"></label><label>Rate %<input name="tax_rate" type="number" min="0" max="100" step=".001" value="0"></label><label>Amount<input name="tax_amount" type="number" min="0" step=".01" value="0"></label><label class="wide">Notes<input name="notes"></label><button class="btn">Add tax / deduction</button></form>`:""}${table(["Tax / deduction","Side","Base / rate","Amount","Actions"],taxes.map((row)=>`<tr><td><strong>${esc(row.tax_name)}</strong><br><small>${esc(label(row.effect))}</small></td><td>${esc(label(row.applies_to))}</td><td>${money(row.taxable_base)} · ${Number(row.tax_rate)}%</td><td>${money(row.tax_amount)}</td><td>${!locked&&can(PERMISSIONS.DELETE)?`<button class="btn btn-sm" type="button" data-delete-procurement-tax="${row.id}">Delete</button>`:"—"}</td></tr>`).join(""),"No other taxes or deductions")}${procurementWizardControls({skip:true})}</section>`;
  }

  if (step === 5 && active) {
    const terms = procurementTermsFor(active, latestApproval);
    const fields = parseProcurementTerms(terms);
    const readOnly = ["pending", "approved"].includes(latestApproval?.status);
    const termActions = terms && can(PERMISSIONS.EDIT) ? `<div class="hp-actions"><button class="btn btn-sm" type="button" data-edit-procurement-terms>Edit terms</button><button class="btn btn-sm" type="button" data-delete-procurement-terms>Delete terms</button></div>` : "";
    content = `<section class="hp-card"><h3>Terms & Conditions · ${esc(active.package_number)}</h3><p class="muted">Define the commercial terms the client must review with this proposal. These terms are included in the client portal approval and PDF.</p>${readOnly ? `${procurementTermsMarkup(terms)}${termActions}<p class="hp-review-note">Revision ${Number(latestApproval.revision_no)} is ${esc(label(latestApproval.status))}. Editing or deleting these terms preserves that revision and starts a new draft.</p>${procurementWizardControls()}` : `<form id="procurementTermsForm" class="hp-form"><label>Payment terms<textarea name="payment_terms" required placeholder="Example: 50% advance, 40% before dispatch, 10% after installation">${esc(fields.payment_terms)}</textarea></label><label>Delivery timeline<textarea name="delivery_timeline" required placeholder="Example: 4-6 weeks from advance and drawing approval">${esc(fields.delivery_timeline)}</textarea></label><label>Installation & commissioning<textarea name="installation_terms" placeholder="Site readiness, unloading, installation and commissioning conditions">${esc(fields.installation_terms)}</textarea></label><label>Warranty & support<textarea name="warranty_terms" placeholder="Warranty period, exclusions and response terms">${esc(fields.warranty_terms)}</textarea></label><label class="wide">Additional terms<textarea name="additional_terms" placeholder="Validity, exclusions, client responsibilities or other clauses">${esc(fields.additional_terms)}</textarea></label><div class="wide hp-actions hp-wizard-actions"><button class="btn" type="button" data-procurement-back>Back</button>${terms && can(PERMISSIONS.EDIT) ? `<button class="btn" type="button" data-delete-procurement-terms>Delete terms</button>` : ""}<span class="hp-wizard-spacer"></span><button class="btn" type="button" data-procurement-skip>Skip for now</button><button class="btn" type="submit">Save terms & continue</button></div></form>`}</section>`;
  }

  if (step === 6 && active) {
    const terms = procurementTermsFor(active, latestApproval);
    const snapshot = ["pending","approved"].includes(latestApproval?.status) ? latestApproval.payload_snapshot : procurementClientSnapshot(active, terms);
    const hasDiscount = (snapshot.items || []).some((item) => Number(item.discount) > 0);
    content = `<section class="hp-card"><div class="hp-actions" style="justify-content:space-between"><div><h3>Client review · ${esc(active.package_number)}</h3><p class="muted">This preview contains client selling values only. Vendor cost and margin are excluded.</p></div><span class="meta-pill">${esc(latestApproval ? `${latestApproval.approval_number} · ${label(latestApproval.status)}` : "Not sent")}</span></div>${table(["Item","Quantity","Unit price","Total price",...(hasDiscount?["Discount"]:[]),"GST","Total inc. GST"], (snapshot.items || []).map((item) => { const amount=procurementItemAmounts(item);return `<tr><td><strong>${esc(item.name)}</strong><br><small>${esc(item.makeModel || "")}</small></td><td>${Number(item.quantity)} ${esc(item.unit)}</td><td>${money(item.unitPrice)}</td><td>${money(amount.unitsTotal)}</td>${hasDiscount?`<td>${money(item.discount)}</td>`:""}<td>${money(amount.gst)} <small>(${Number(item.gstRate || 0)}%)</small></td><td><strong>${money(amount.totalIncGst)}</strong></td></tr>`; }).join(""), "No client-priced items")}<div class="hp-kpis">${kpi("Items", money(snapshot.totals?.itemsSubtotal), "Taxable item value")}${kpi("Charges", money(snapshot.totals?.chargesSubtotal), "Transport / installation")}${kpi("GST", money(snapshot.totals?.gst), "Added or extracted")}${kpi("Grand total", money(snapshot.totals?.grandTotal), "Client approval amount")}</div>${procurementTermsMarkup(snapshot.terms)}<form id="procurementApprovalForm" class="hp-form"><div class="wide hp-actions"><button class="btn" id="downloadProcurementProposal" type="button">Download client PDF</button>${!latestApproval && can(PERMISSIONS.CREATE) ? `<button class="btn" type="submit">Send to client portal for approval</button>` : ""}${latestApproval && ["revision_requested","rejected","superseded"].includes(latestApproval.status) && can(PERMISSIONS.CREATE) ? `<button class="btn" type="submit">Send revised proposal</button>` : ""}${purchaseOrder ? `<button class="btn" id="downloadPurchaseOrder" type="button">Download ${esc(purchaseOrder.po_number)}</button>` : `<button class="btn" id="createPurchaseOrder" type="button" ${latestApproval?.status === "approved" ? "" : "disabled title=\"Client approval required\""}>Generate purchase order</button>`}</div></form>${latestApproval?.decision_remarks ? `<p class="hp-review-note"><strong>Client remarks:</strong> ${esc(latestApproval.decision_remarks)}</p>` : ""}${procurementWizardControls({ next: false })}</section>`;
  }

  if (step >= 3 && !active) content = `<section class="hp-card"><h3>Select a vendor package first</h3><p>Return to Step 2 and select the package you want to price.</p><button class="btn" type="button" data-procurement-step="2">Go to vendor packages</button></section>`;
  return `${projectHeader()}${stepper}${active ? `<div class="hp-actions" style="margin-bottom:1rem"><span class="meta-pill">Working package: ${esc(active.package_number)} · ${esc(active.vendor_name)}</span><button class="btn btn-sm" id="changeProcurementPackage" type="button">Change package</button></div>` : ""}${content}`;
}

function licensingPage() {
  const vendorOptions = state.vendors.map((x) => `<option value="${x.id}">${esc(x.vendor_code)} · ${esc(x.legal_name)}</option>`).join("");
  return `${projectHeader()}${can(PERMISSIONS.CREATE) ? `<section class="hp-card"><h3>Add licence or compliance requirement</h3><p class="muted">Manage the requirement inside this project workspace.</p><form id="projectLicenseForm" class="hp-form"><label>Licence / approval<input name="license_name" required></label><label>Authority<input name="authority_name" required></label><label>Responsible consultant<select name="responsible_vendor_id"><option value="">Internal</option>${vendorOptions}</select></label><label>Statutory fee<input name="statutory_fee" type="number" min="0" step=".01" value="0"></label><label>Vendor service cost<input name="vendor_service_cost" type="number" min="0" step=".01" value="0"></label><label>Client selling price<input name="client_price" type="number" min="0" step=".01" value="0"></label><label>Target date<input name="target_date" type="date"></label><label class="wide">Notes<textarea name="notes"></textarea></label><button class="btn">Add requirement</button></form></section>` : ""}<section class="hp-card"><h3>Licensing & Compliance</h3><p class="muted">Project-scoped statutory and compliance register.</p>${table(["Requirement", "Authority", "Consultant", "Target", "Client price", "Status"], state.licenses.map((x) => `<tr><td><strong>${esc(x.license_name)}</strong></td><td>${esc(x.authority_name)}</td><td>${esc(x.hospital_vendors?.legal_name || "Internal")}</td><td>${date(x.target_date)}</td><td>${money(x.client_price)}</td><td>${esc(label(x.status))}</td></tr>`).join(""), "No licence requirements")}</section>`;
}

function contractorsPage() {
  const vendorOptions = state.vendors.map((x) => `<option value="${x.id}">${esc(x.vendor_code)} · ${esc(x.legal_name)}</option>`).join("");
  return `${projectHeader()}${can(PERMISSIONS.CREATE) ? `<section class="hp-card"><h3>Assign contractor or specialist vendor</h3><form id="contractorForm" class="hp-form"><label>Vendor<select name="vendor_id" required><option value="">Select</option>${vendorOptions}</select></label><label>Package name<input name="package_name" required></label><label>Awarded value<input name="awarded_value" type="number" min="0" step=".01"></label><label class="wide">Scope summary<textarea name="scope_summary"></textarea></label><button class="btn">Assign vendor</button></form></section>` : ""}<section class="hp-card"><h3>Project contractors</h3>${table(["Vendor", "Package", "Scope", "Awarded value", "Status"], state.assignments.map((x) => `<tr><td><strong>${esc(x.hospital_vendors?.legal_name || "—")}</strong><br><small>${esc(x.hospital_vendors?.vendor_code || "")}</small></td><td>${esc(x.package_name)}</td><td>${esc(x.scope_summary || "—")}</td><td>${money(x.awarded_value)}</td><td>${esc(label(x.status))}</td></tr>`).join(""), "No contractors assigned")}</section>`;
}

function documentsPage() {
  const foldersReady=state.project.drive_folder_status==="ready";
  const canDeleteDocuments=can(PERMISSIONS.DELETE);
  return `${projectHeader()}<section class="hp-card"><h3>Google Drive project archive</h3><p class="muted">Files use the dedicated Hospital Projects Drive root and are stored inside this project's folder hierarchy.</p><div class="hp-actions"><span class="meta-pill">Folder: ${esc(label(state.project.drive_folder_status))}</span>${state.project.drive_folder_path ? `<span class="meta-pill">${esc(state.project.drive_folder_path)}</span>` : ""}${foldersReady?"":`<button class="btn" id="ensureDriveFolders" type="button">Prepare folders</button>`}</div>${can(PERMISSIONS.CREATE) ? `<form id="documentForm" class="hp-form" style="margin-top:1rem"><label>Document type<select name="document_type"><option>Documents</option><option>Planning & Design</option><option>Construction</option><option>Procurement</option><option>Procurement Proposals</option><option>Purchase Orders</option><option>Medical Equipment</option><option>Licensing & Compliance</option><option>Contractors</option><option>Client Invoices</option><option>Client Receipts</option><option>Credit Notes</option><option>Vendor Bills</option><option>Vendor Payments</option><option>Commercial</option></select></label><label class="span2">Files<input name="files" type="file" multiple required></label><button class="btn">Upload to Google Drive</button></form>` : ""}</section><section class="hp-card"><h3>Project documents</h3>${table(["File", "Type", "Uploaded", "Status", "Actions"], state.documents.map((x) => `<tr><td><strong>${esc(x.file_name)}</strong></td><td>${esc(x.document_type || "Documents")}</td><td>${date(x.created_at)}</td><td>${esc(label(x.upload_status))}</td><td><div class="hp-actions">${x.web_view_link ? `<a class="btn btn-sm" target="_blank" rel="noopener" href="${esc(x.web_view_link)}">Open</a>` : ""}${canDeleteDocuments&&x.drive_file_id?`<button class="btn btn-sm" type="button" data-delete-project-document="${x.id}">Delete</button>`:""}${!x.web_view_link&&!(canDeleteDocuments&&x.drive_file_id)?"—":""}</div></td></tr>`).join(""), "No documents uploaded")}</section>`;
}

function clientPaymentsPage() {
  const today = new Date().toISOString().slice(0, 10);
  const editing=state.invoices.find((x)=>x.id===state.billingEditingId)||null;
  const editLines=editing?state.invoiceLines.filter((x)=>x.invoice_id===editing.id):[{description:"",hsn_sac:"",quantity:1,unit:"Nos",unit_price:0,discount_amount:0,gst_rate:18,gst_included:false}];
  const line=(x,index)=>`<tr class="hospital-billing-line"><td><input name="description" value="${esc(x.description)}" required placeholder="Item / service"></td><td><input name="hsn_sac" value="${esc(x.hsn_sac||"")}" placeholder="HSN/SAC"></td><td><input name="quantity" type="number" min=".001" step=".001" value="${Number(x.quantity||1)}" required></td><td><input name="unit" value="${esc(x.unit||"Nos")}" required></td><td><input name="unit_price" type="number" min="0" step=".01" value="${Number(x.unit_price||0)}" required></td><td><input name="discount_amount" type="number" min="0" step=".01" value="${Number(x.discount_amount||0)}"></td><td><input name="gst_rate" type="number" min="0" max="100" step=".001" value="${Number(x.gst_rate??18)}"></td><td><select name="gst_included"><option value="false" ${x.gst_included?"":"selected"}>GST extra</option><option value="true" ${x.gst_included?"selected":""}>GST included</option></select></td><td class="billing-line-total">${money(x.total_amount||0)}</td><td><button class="btn btn-sm" type="button" data-remove-billing-line ${index===0&&editLines.length===1?"disabled":""}>Remove</button></td></tr>`;
  const invoiceRows=state.invoices.map((x)=>{const doc=state.documents.find((d)=>d.entity_type==="hospital_invoices"&&d.entity_id===x.id);const editable=x.status==="draft"&&x.accounts_status==="unposted"&&!Number(x.paid_amount)&&!Number(x.credited_amount);return `<tr><td><strong>${esc(x.invoice_number)}</strong><br><small>${esc(label(x.invoice_type))}</small></td><td>${date(x.issue_date)}<br><small>Due ${date(x.due_date)}</small></td><td>${money(x.total_amount)}</td><td>${money(x.paid_amount)}</td><td>${money(x.credited_amount)}</td><td>${money(x.balance_amount)}</td><td>${esc(label(x.status))}<br><small>Accounts: ${esc(label(x.accounts_status))}</small></td><td><div class="hp-actions">${doc?.web_view_link?`<a class="btn btn-sm" href="${esc(doc.web_view_link)}" target="_blank" rel="noopener">Drive</a>`:""}<button class="btn btn-sm" type="button" data-download-invoice="${x.id}">PDF</button>${editable&&can(PERMISSIONS.EDIT)?`<button class="btn btn-sm" type="button" data-edit-invoice="${x.id}">Edit</button>`:""}${editable&&can(PERMISSIONS.DELETE)?`<button class="btn btn-sm" type="button" data-delete-invoice="${x.id}">Delete</button>`:""}${Number(x.balance_amount)>0&&can(PERMISSIONS.CREATE)?`<button class="btn btn-sm" type="button" data-new-credit="${x.id}">Credit note</button>`:""}</div></td></tr>`}).join("");
  const creditEditing=state.credits.find((x)=>x.id===state.creditEditingId)||(String(state.creditEditingId||"").startsWith("new:")?{selectedInvoiceId:String(state.creditEditingId).slice(4)}:null);
  const creditRows=state.credits.map((x)=>{const doc=state.documents.find((d)=>d.entity_type==="hospital_credit_notes"&&d.entity_id===x.id),editable=x.status==="draft"&&x.accounts_status==="unposted";return `<tr><td><strong>${esc(x.credit_note_number)}</strong></td><td>${esc(x.hospital_invoices?.invoice_number||"—")}</td><td>${date(x.credit_note_date)}</td><td>${esc(x.reason)}</td><td>${money(x.amount)}</td><td>${esc(label(x.status))}<br><small>Accounts: ${esc(label(x.accounts_status))}</small></td><td><div class="hp-actions"><button class="btn btn-sm" type="button" data-view-credit="${x.id}">View</button><button class="btn btn-sm" type="button" data-download-credit="${x.id}">PDF</button>${editable&&can(PERMISSIONS.EDIT)?`<button class="btn btn-sm" type="button" data-edit-credit="${x.id}">Edit</button>`:""}${editable&&can(PERMISSIONS.DELETE)?`<button class="btn btn-sm" type="button" data-delete-credit="${x.id}">Delete</button>`:""}</div></td></tr>`}).join("");
  return `${projectHeader()}${can(PERMISSIONS.CREATE)||editing?`<section class="hp-card"><div class="hp-section-heading"><div><span class="hp-eyebrow">ADVANCED BILL GENERATOR</span><h3>${editing?`Edit ${esc(editing.invoice_number)}`:"Create client invoice"}</h3><p>Itemized GST billing with centralized numbering, project Drive archival and Central Accounts staging.</p></div></div><form id="projectInvoiceForm"><input type="hidden" name="invoice_id" value="${esc(editing?.id||"")}"><div class="hp-form"><label>Invoice type<select name="invoice_type">${["advance","progress","retention","final","other"].map(v=>`<option value="${v}" ${(editing?.invoice_type||"progress")===v?"selected":""}>${label(v)}</option>`).join("")}</select></label><label>Issue date<input name="issue_date" type="date" value="${editing?.issue_date||today}" required></label><label>Due date<input name="due_date" type="date" value="${editing?.due_date||""}"></label><label>Client reference / PO<input name="reference_number" value="${esc(editing?.reference_number||"")}"></label><label>Place of supply<input name="place_of_supply" value="${esc(editing?.place_of_supply||state.project.location||"")}"></label><label>Round off<input name="round_off" type="number" step=".01" value="${Number(editing?.round_off||0)}"></label><label class="wide">Payment terms<textarea name="payment_terms">${esc(editing?.payment_terms||"")}</textarea></label><label class="wide">Notes<textarea name="notes">${esc(editing?.notes||"")}</textarea></label></div><div class="table-container hp-billing-editor"><table><thead><tr><th>Description</th><th>HSN/SAC</th><th>Qty</th><th>Unit</th><th>Unit price</th><th>Discount</th><th>GST %</th><th>GST method</th><th>Total</th><th></th></tr></thead><tbody id="hospitalBillingLines">${editLines.map(line).join("")}</tbody></table></div><div class="hp-billing-summary"><span>Taxable <strong id="billingTaxable">₹0</strong></span><span>GST <strong id="billingGst">₹0</strong></span><span>Grand total <strong id="billingGrandTotal">₹0</strong></span></div><div class="hp-actions"><button class="btn" id="addBillingLine" type="button">Add line</button><button class="btn" name="save_action" value="issue">Issue Invoice</button>${editing?`<button class="btn" id="cancelInvoiceEdit" type="button">Cancel edit</button>`:""}</div></form></section>`:""}${creditEditing?creditNoteForm(creditEditing,today):""}<section class="hp-card"><h3>Invoices</h3><p class="muted">Issued invoices are read by Central Accounts. Only unpaid, unposted drafts can be edited or hard-deleted.</p>${table(["Invoice","Issue / due","Total","Paid","Credited","Balance","Status","Actions"],invoiceRows,"No client invoices generated")}</section><section class="hp-card"><h3>Credit notes</h3><p class="muted">Credit notes use the centralized Hospital credit-note sequence and their own Drive PDF.</p>${table(["Credit note","Against invoice","Date","Reason","Total","Status","Actions"],creditRows,"No credit notes issued")}</section>`;
}

function creditNoteForm(note,today=new Date().toISOString().slice(0,10)) { const invoice=state.invoices.find((x)=>x.id===(note?.invoice_id||note?.selectedInvoiceId));return `<section class="hp-card" id="creditNoteEditor"><div class="hp-section-heading"><div><span class="hp-eyebrow">CREDIT NOTE GENERATOR</span><h3>${note?.id?`Edit ${esc(note.credit_note_number)}`:"Issue credit note"}</h3><p>Against ${esc(invoice?.invoice_number||"selected invoice")}; available balance ${money(invoice?.balance_amount||0)}.</p></div></div><form id="creditNoteForm" class="hp-form"><input type="hidden" name="credit_note_id" value="${esc(note?.id||"")}"><input type="hidden" name="invoice_id" value="${esc(note?.invoice_id||note?.selectedInvoiceId||"")}"><label>Date<input name="credit_note_date" type="date" value="${note?.credit_note_date||today}" required></label><label>Taxable credit<input name="taxable_amount" type="number" min=".01" step=".01" value="${Number(note?.taxable_amount||0)}" required></label><label>GST rate %<input name="tax_rate" type="number" min="0" max="100" step=".001" value="${Number(note?.tax_rate??18)}" required></label><label class="wide">Reason<textarea name="reason" required>${esc(note?.reason||"")}</textarea></label><label class="wide">Notes<textarea name="notes">${esc(note?.notes||"")}</textarea></label><div class="hp-actions wide"><button class="btn" name="save_action" value="approve">Issue Credit Note</button><button class="btn" id="cancelCreditEdit" type="button">Cancel</button></div></form></section>`; }
const QUOTATION_STATUS_LABEL = { draft: "Draft", sent: "Sent", accepted: "Accepted", rejected: "Rejected", expired: "Expired" };
function quotationLineAmounts(line) {
  const quantity = Number(line.quantity) || 0, unitPrice = Number(line.unit_price) || 0, discount = Number(line.discount_amount) || 0, gstRate = Number(line.gst_rate) || 0;
  const entered = Math.max(0, quantity * unitPrice - discount);
  const included = line.gst_included === true || line.gst_included === "true";
  const taxable = included && gstRate > 0 ? entered * 100 / (100 + gstRate) : entered;
  const tax = included ? entered - taxable : taxable * gstRate / 100;
  return { taxable, tax, total: taxable + tax };
}
function quotationsPage() {
  const rows = state.quotations.map((x) => {
    const editable = x.status === "draft";
    return `<tr><td><strong>${esc(x.quotation_number)}</strong><br><small>${esc(x.subject || "")}</small></td><td>${date(x.quotation_date)}<br><small>Valid till ${date(x.valid_until)}</small></td><td>${money(x.total_amount)}</td><td>${esc(QUOTATION_STATUS_LABEL[x.status] || label(x.status))}</td><td><div class="hp-actions">
      <button class="btn btn-sm" type="button" data-download-quotation="${x.id}">PDF</button>
      ${editable && can(PERMISSIONS.EDIT) ? `<button class="btn btn-sm" type="button" data-edit-quotation="${x.id}">Edit</button>` : ""}
      ${x.status === "draft" && can(PERMISSIONS.EDIT) ? `<button class="btn btn-sm" type="button" data-quotation-status="${x.id}:sent">Mark Sent</button>` : ""}
      ${x.status === "sent" && can(PERMISSIONS.EDIT) ? `<button class="btn btn-sm" type="button" data-quotation-status="${x.id}:accepted">Accepted</button><button class="btn btn-sm" type="button" data-quotation-status="${x.id}:rejected">Rejected</button>` : ""}
      ${editable && can(PERMISSIONS.DELETE) ? `<button class="btn btn-sm" type="button" data-delete-quotation="${x.id}">Delete</button>` : ""}
    </div></td></tr>`;
  }).join("");
  return `${projectHeader()}<section class="hp-card hp-billing-launch"><div><span class="hp-eyebrow">CLIENT QUOTATIONS</span><h3>Branded project quotations</h3><p>Itemised GST per line and overall, company letterhead, and editable terms &amp; conditions. Only draft quotations can be edited or deleted.</p></div><div class="hp-actions">${can(PERMISSIONS.CREATE) ? `<button class="btn" id="openQuotationGenerator" type="button">New Quotation</button>` : ""}</div></section><section class="hp-card"><h3>Quotations</h3>${table(["Quotation", "Date / Validity", "Total", "Status", "Actions"], rows, "No quotations generated yet")}</section>${quotationModal()}`;
}
function quotationModal() {
  if (!state.quotationModal) return "";
  const today = new Date().toISOString().slice(0, 10);
  const editing = state.quotations.find((x) => x.id === state.quotationEditingId) || null;
  const editLines = editing ? state.quotationLines.filter((x) => x.quotation_id === editing.id) : [{ item_name: "", description: "", hsn_sac: "", quantity: 1, unit: "Nos", unit_price: 0, discount_amount: 0, gst_rate: 18, gst_included: false }];
  const rows = editLines.map((x, index) => `<tr class="hospital-quotation-line"><td>${index + 1}</td><td class="hp-billing-item-cell"><input name="item_name" value="${esc(x.item_name)}" required placeholder="Item / equipment name"><textarea name="description" placeholder="Description (shown below item in PDF)">${esc(x.description || "")}</textarea></td><td><input name="hsn_sac" value="${esc(x.hsn_sac || "")}" placeholder="HSN/SAC"></td><td><input name="quantity" type="number" min=".001" step=".001" value="${Number(x.quantity || 1)}" required><input name="unit" value="${esc(x.unit || "Nos")}" required></td><td><input name="unit_price" type="number" min="0" step=".01" value="${Number(x.unit_price || 0)}" required></td><td><input name="discount_amount" type="number" min="0" step=".01" value="${Number(x.discount_amount || 0)}"></td><td><input name="gst_rate" type="number" min="0" max="100" step=".001" value="${Number(x.gst_rate ?? 18)}"><select name="gst_included"><option value="false" ${x.gst_included ? "" : "selected"}>Extra</option><option value="true" ${x.gst_included ? "selected" : ""}>Included</option></select></td><td class="billing-line-total">${money(quotationLineAmounts(x).total)}</td><td><button class="hp-paper-remove" type="button" data-remove-quotation-line aria-label="Remove line" ${index === 0 && editLines.length === 1 ? "disabled" : ""}>×</button></td></tr>`).join("");
  const client = state.project.hospital_clients || {};
  return `<div class="hp-billing-modal" role="dialog" aria-modal="true" aria-labelledby="quotationDocumentTitle"><div class="hp-billing-modal-shell"><div class="hp-billing-modal-toolbar"><div><strong>${editing ? `Edit ${esc(editing.quotation_number)}` : "Generate quotation"}</strong><small>PDF-matched document editor</small></div><button class="btn" type="button" data-close-quotation-modal>Close</button></div><form id="projectQuotationForm" class="hp-paper"><input type="hidden" name="quotation_id" value="${esc(editing?.id || "")}"><header class="hp-paper-header"><div class="hp-paper-company"><img src="/new-ems/assets/pdf/vn-logo.png" alt="Varada Nexus"><div><strong>VARADA NEXUS PRIVATE LIMITED</strong><small>GST: 37AAKCV7495B1ZV</small><small>K B Nagar, A V A Road, Rajahmundry, Andhra Pradesh - 533101</small><small>CIN: U43121AP2025PTC117741</small></div></div><div class="hp-paper-title"><span>Digitally Verifiable</span><h2 id="quotationDocumentTitle">QUOTATION</h2><label>Quotation No.<input value="${esc(editing?.quotation_number || "Generated on save")}" readonly></label><label>Quotation date<input name="quotation_date" type="date" value="${editing?.quotation_date || today}" required></label><label>Valid until<input name="valid_until" type="date" value="${editing?.valid_until || ""}"></label></div></header><section class="hp-paper-section"><h3>QUOTED TO & PROJECT</h3><div class="hp-paper-details"><label>Client<input value="${esc(client.hospital_name || "")}" readonly></label><label>Client code<input value="${esc(client.client_code || "")}" readonly></label><label>Project<input value="${esc(`${state.project.project_code} · ${state.project.title}`)}" readonly></label><label>GSTIN<input value="${esc(client.gstin || "N/A")}" readonly></label><label>Place of supply<input name="place_of_supply" value="${esc(editing?.place_of_supply || state.project.location || "")}"></label><label class="span2">Subject<input name="subject" maxlength="200" value="${esc(editing?.subject || "")}" placeholder="What is this quotation for?"></label></div></section><section class="hp-paper-section hp-paper-lines"><table><thead><tr><th>#</th><th>Item &amp; description</th><th>HSN/SAC</th><th>Qty / Unit</th><th>Unit price</th><th>Discount</th><th>GST</th><th>Total inc. GST</th><th></th></tr></thead><tbody id="hospitalQuotationLines">${rows}</tbody></table><button class="hp-paper-add" id="addQuotationLine" type="button">+ Add quotation line</button></section><section class="hp-paper-commercial"><div class="hp-paper-terms"><label>Terms &amp; conditions<textarea name="terms_conditions" rows="8">${esc(editing?.terms_conditions ?? DEFAULT_QUOTATION_TERMS)}</textarea></label><label>Notes<textarea name="notes" placeholder="Delivery, warranty or other notes">${esc(editing?.notes || "")}</textarea></label><label class="hp-checkbox-inline"><input type="checkbox" name="include_signature" value="true" ${editing?.include_signature === false ? "" : "checked"}> Include signature &amp; company stamp on the PDF</label></div><div class="hp-paper-totals"><label>Round off<input name="round_off" type="number" step=".01" value="${Number(editing?.round_off || 0)}"></label><span>Taxable value<strong id="quotationTaxable">₹0</strong></span><span>GST<strong id="quotationGst">₹0</strong></span><span class="grand">GRAND TOTAL<strong id="quotationGrandTotal">₹0</strong></span></div></section><footer class="hp-paper-signature"><div><span>For Varada Nexus Private Limited</span><strong>Authorised Signatory</strong></div><div class="hp-paper-stamp"><img src="/new-ems/assets/pdf/vn-stamp.png" alt="Company stamp"><img src="/new-ems/assets/pdf/vn-signature.png" alt="Authorised signature"></div></footer><div class="hp-paper-actions"><button class="btn" name="save_action" value="draft">Save Quotation</button>${editing ? `<button class="btn" id="cancelQuotationEdit" type="button">Cancel edit</button>` : ""}</div></form></div></div>`;
}
function billingInvoiceModal() {
  if(state.billingModal!=="invoice")return "";
  const today=new Date().toISOString().slice(0,10),editing=state.invoices.find((x)=>x.id===state.billingEditingId)||null;
  const editLines=editing?state.invoiceLines.filter((x)=>x.invoice_id===editing.id):[{description:"",hsn_sac:"",quantity:1,unit:"Nos",unit_price:0,discount_amount:0,gst_rate:18,gst_included:false}];
  const rows=editLines.map((x,index)=>`<tr class="hospital-billing-line"><td>${index+1}</td><td><input name="description" value="${esc(x.description)}" required placeholder="Item / service description"></td><td><input name="hsn_sac" value="${esc(x.hsn_sac||"")}" placeholder="HSN/SAC"></td><td><input name="quantity" type="number" min=".001" step=".001" value="${Number(x.quantity||1)}" required><input name="unit" value="${esc(x.unit||"Nos")}" required></td><td><input name="unit_price" type="number" min="0" step=".01" value="${Number(x.unit_price||0)}" required></td><td><input name="discount_amount" type="number" min="0" step=".01" value="${Number(x.discount_amount||0)}"></td><td><input name="gst_rate" type="number" min="0" max="100" step=".001" value="${Number(x.gst_rate??18)}"><select name="gst_included"><option value="false" ${x.gst_included?"":"selected"}>Extra</option><option value="true" ${x.gst_included?"selected":""}>Included</option></select></td><td class="billing-line-total">${money(x.total_amount||0)}</td><td><button class="hp-paper-remove" type="button" data-remove-billing-line aria-label="Remove line" ${index===0&&editLines.length===1?"disabled":""}>×</button></td></tr>`).join("");
  const client=state.project.hospital_clients||{};
  return `<div class="hp-billing-modal" role="dialog" aria-modal="true" aria-labelledby="billingDocumentTitle"><div class="hp-billing-modal-shell"><div class="hp-billing-modal-toolbar"><div><strong>${editing?`Edit ${esc(editing.invoice_number)}`:"Generate invoice"}</strong><small>PDF-matched document editor</small></div><button class="btn" type="button" data-close-billing-modal>Close</button></div><form id="projectInvoiceForm" class="hp-paper"><input type="hidden" name="invoice_id" value="${esc(editing?.id||"")}"><header class="hp-paper-header"><div class="hp-paper-company"><img src="/new-ems/assets/pdf/vn-logo.png" alt="Varada Nexus"><div><strong>VARADA NEXUS PRIVATE LIMITED</strong><small>GST: 37AAKCV7495B1ZV</small><small>K B Nagar, A V A Road, Rajahmundry, Andhra Pradesh - 533101</small><small>CIN: U43121AP2025PTC117741</small></div></div><div class="hp-paper-title"><span>Digitally Verifiable</span><h2 id="billingDocumentTitle">TAX INVOICE</h2><label>Invoice No.<input value="${esc(editing?.invoice_number||"Generated on save")}" readonly></label><label>Issue date<input name="issue_date" type="date" value="${editing?.issue_date||today}" required></label><label>Due date<input name="due_date" type="date" value="${editing?.due_date||""}"></label></div></header><section class="hp-paper-section"><h3>BILL TO & PROJECT</h3><div class="hp-paper-details"><label>Client<input value="${esc(client.hospital_name||"")}" readonly></label><label>Client code<input value="${esc(client.client_code||"")}" readonly></label><label>Billing address<textarea readonly>${esc(client.billing_address||"")}</textarea></label><label>GSTIN<input value="${esc(client.gstin||"N/A")}" readonly></label><label>Project<input value="${esc(`${state.project.project_code} · ${state.project.title}`)}" readonly></label><label>Place of supply<input name="place_of_supply" value="${esc(editing?.place_of_supply||state.project.location||"")}"></label><label>Invoice type<select name="invoice_type">${["advance","progress","retention","final","other"].map(v=>`<option value="${v}" ${(editing?.invoice_type||"progress")===v?"selected":""}>${label(v)}</option>`).join("")}</select></label><label>Client reference / PO<input name="reference_number" value="${esc(editing?.reference_number||"")}"></label></div></section><section class="hp-paper-section hp-paper-lines"><table><thead><tr><th>#</th><th>Description</th><th>HSN/SAC</th><th>Qty / Unit</th><th>Unit price</th><th>Discount</th><th>GST</th><th>Total inc. GST</th><th></th></tr></thead><tbody id="hospitalBillingLines">${rows}</tbody></table><button class="hp-paper-add" id="addBillingLine" type="button">+ Add invoice line</button></section><section class="hp-paper-commercial"><div class="hp-paper-terms"><label>Payment terms<textarea name="payment_terms" placeholder="Payment schedule and conditions">${esc(editing?.payment_terms||"")}</textarea></label><label>Notes<textarea name="notes" placeholder="Delivery, warranty or other notes">${esc(editing?.notes||"")}</textarea></label></div><div class="hp-paper-totals"><label>Round off<input name="round_off" type="number" step=".01" value="${Number(editing?.round_off||0)}"></label><span>Taxable value<strong id="billingTaxable">₹0</strong></span><span>GST<strong id="billingGst">₹0</strong></span><span class="grand">GRAND TOTAL<strong id="billingGrandTotal">₹0</strong></span></div></section><footer class="hp-paper-signature"><div><span>For Varada Nexus Private Limited</span><strong>Authorised Signatory</strong></div><div class="hp-paper-stamp"><img src="/new-ems/assets/pdf/vn-stamp.png" alt="Company stamp"><img src="/new-ems/assets/pdf/vn-signature.png" alt="Authorised signature"></div></footer><div class="hp-paper-actions"><button class="btn" name="save_action" value="issue">Issue Invoice</button>${editing?`<button class="btn" id="cancelInvoiceEdit" type="button">Cancel edit</button>`:""}</div></form></div></div>`;
}

function billingCreditModal() {
  if(state.billingModal!=="credit")return "";
  const today=new Date().toISOString().slice(0,10),note=state.credits.find((x)=>x.id===state.creditEditingId)||null,preselected=note?.invoice_id||(String(state.creditEditingId||"").startsWith("new:")?String(state.creditEditingId).slice(4):"");
  const invoiceOptions=state.invoices.filter((x)=>Number(x.balance_amount)>0||x.id===preselected).map((x)=>`<option value="${x.id}" ${x.id===preselected?"selected":""}>${esc(x.invoice_number)} · ${money(x.balance_amount)} available</option>`).join(""),client=state.project.hospital_clients||{};
  return `<div class="hp-billing-modal" role="dialog" aria-modal="true" aria-labelledby="creditDocumentTitle"><div class="hp-billing-modal-shell"><div class="hp-billing-modal-toolbar"><div><strong>${note?`Edit ${esc(note.credit_note_number)}`:"Generate credit note"}</strong><small>PDF-matched document editor</small></div><button class="btn" type="button" data-close-billing-modal>Close</button></div><form id="creditNoteForm" class="hp-paper"><input type="hidden" name="credit_note_id" value="${esc(note?.id||"")}"><header class="hp-paper-header"><div class="hp-paper-company"><img src="/new-ems/assets/pdf/vn-logo.png" alt="Varada Nexus"><div><strong>VARADA NEXUS PRIVATE LIMITED</strong><small>GST: 37AAKCV7495B1ZV</small><small>K B Nagar, A V A Road, Rajahmundry, Andhra Pradesh - 533101</small><small>CIN: U43121AP2025PTC117741</small></div></div><div class="hp-paper-title"><span>Digitally Verifiable</span><h2 id="creditDocumentTitle">CREDIT NOTE</h2><label>Credit Note No.<input value="${esc(note?.credit_note_number||"Generated on save")}" readonly></label><label>Date<input name="credit_note_date" type="date" value="${note?.credit_note_date||today}" required></label></div></header><section class="hp-paper-section"><h3>BILL TO & PROJECT</h3><div class="hp-paper-details"><label>Client<input value="${esc(client.hospital_name||"")}" readonly></label><label>Client code<input value="${esc(client.client_code||"")}" readonly></label><label>Project<input value="${esc(`${state.project.project_code} · ${state.project.title}`)}" readonly></label><label>GSTIN<input value="${esc(client.gstin||"N/A")}" readonly></label><label class="span2">Against invoice<select name="invoice_id" required><option value="">Select an invoice</option>${invoiceOptions}</select></label></div></section><section class="hp-paper-section"><h3>CREDIT DETAILS</h3><div class="hp-paper-credit-grid"><label class="span2">Reason<textarea name="reason" required placeholder="Reason for issuing this credit note">${esc(note?.reason||"")}</textarea></label><label>Taxable credit<input name="taxable_amount" type="number" min=".01" step=".01" value="${Number(note?.taxable_amount||0)}" required></label><label>GST rate %<input name="tax_rate" type="number" min="0" max="100" step=".001" value="${Number(note?.tax_rate??18)}" required></label><label class="span2">Notes<textarea name="notes">${esc(note?.notes||"")}</textarea></label></div><div class="hp-paper-credit-total"><span>Taxable value<strong id="creditTaxable">${money(note?.taxable_amount||0)}</strong></span><span>GST<strong id="creditGst">${money(note?.tax_amount||0)}</strong></span><span class="grand">TOTAL CREDIT<strong id="creditGrandTotal">${money(note?.amount||0)}</strong></span></div></section><footer class="hp-paper-signature"><div class="hp-paper-authorisation"><span>For Varada Nexus Private Limited</span><div class="hp-paper-stamp"><img src="/new-ems/assets/pdf/vn-stamp.png" alt="Company stamp"><img src="/new-ems/assets/pdf/vn-signature.png" alt="Authorised signature"></div><strong>Authorised Signatory</strong></div></footer><div class="hp-paper-actions"><button class="btn" name="save_action" value="approve">Issue Credit Note</button><button class="btn" id="cancelCreditEdit" type="button">Cancel</button></div></form></div></div>`;
}

function billingPdfPreviewModal() {
  if(!state.billingPreview?.url)return "";
  return `<div class="hp-native-pdf-modal" role="dialog" aria-modal="true" aria-labelledby="billingPreviewTitle"><div class="hp-native-pdf-shell"><header><div><span class="hp-eyebrow">DOCUMENT PREVIEW</span><h3 id="billingPreviewTitle">${esc(state.billingPreview.title||"Billing document")}</h3></div><div class="hp-actions"><button class="btn" id="downloadBillingPreview" type="button">Download PDF</button><button class="btn" data-close-billing-preview type="button">Close</button></div></header><iframe src="${esc(state.billingPreview.url)}" title="${esc(state.billingPreview.title||"Billing document")}"></iframe></div></div>`;
}

function billingInvoiceModalV2() {
  if(state.billingModal!=="invoice")return "";
  const today=new Date().toISOString().slice(0,10),editing=state.invoices.find((x)=>x.id===state.billingEditingId)||null;
  const gstMode=editing?.gst_mode||"item_wise",commonRate=Number(editing?.common_gst_rate??18),commonIncluded=Boolean(editing?.common_gst_included);
  const editLines=editing?state.invoiceLines.filter((x)=>x.invoice_id===editing.id):[{item_name:"",description:"",hsn_sac:"",quantity:1,unit:"Nos",unit_price:0,discount_amount:0,gst_rate:18,gst_included:false}];
  const row=(x,index)=>`<tr class="hospital-billing-line"><td>${index+1}</td><td class="hp-billing-item-cell"><input name="item_name" value="${esc(x.item_name||(!editing?"":x.description||""))}" required placeholder="Item / service name"><textarea name="description" placeholder="Description (shown below item in PDF)">${esc(x.item_name?x.description||"":"")}</textarea></td><td><input name="hsn_sac" value="${esc(x.hsn_sac||"")}" placeholder="HSN/SAC"></td><td><input name="quantity" type="number" min=".001" step=".001" value="${Number(x.quantity||1)}" required><input name="unit" value="${esc(x.unit||"Nos")}" required></td><td><input name="unit_price" type="number" min="0" step=".01" value="${Number(x.unit_price||0)}" required></td><td><input name="discount_amount" type="number" min="0" step=".01" value="${Number(x.discount_amount||0)}" placeholder="Optional"></td><td class="hp-line-gst" ${gstMode==="common"?'hidden':""}><input name="gst_rate" type="number" min="0" max="100" step=".001" value="${Number(x.gst_rate??18)}"><select name="gst_included"><option value="false" ${x.gst_included?"":"selected"}>Extra</option><option value="true" ${x.gst_included?"selected":""}>Included</option></select></td><td class="hp-common-gst-label" ${gstMode==="common"?"":'hidden'}>Common bill GST</td><td class="billing-line-total">${money(x.total_amount||0)}</td><td><button class="hp-paper-remove" type="button" data-remove-billing-line aria-label="Remove line" ${index===0&&editLines.length===1?"disabled":""}>×</button></td></tr>`;
  const client=state.project.hospital_clients||{};
  return `<div class="hp-billing-modal" role="dialog" aria-modal="true" aria-labelledby="billingDocumentTitle"><div class="hp-billing-modal-shell"><div class="hp-billing-modal-toolbar"><div><strong>${editing?`Edit ${esc(editing.invoice_number)}`:"Generate invoice"}</strong><small>PDF-matched document editor</small></div><button class="btn" type="button" data-close-billing-modal>Close</button></div><form id="projectInvoiceForm" class="hp-paper"><input type="hidden" name="invoice_id" value="${esc(editing?.id||"")}"><header class="hp-paper-header"><div class="hp-paper-company"><img src="/new-ems/assets/pdf/vn-logo.png" alt="Varada Nexus"><div><strong>VARADA NEXUS PRIVATE LIMITED</strong><small>GST: 37AAKCV7495B1ZV</small><small>K B Nagar, A V A Road, Rajahmundry, Andhra Pradesh - 533101</small><small>CIN: U43121AP2025PTC117741</small></div></div><div class="hp-paper-title"><span>Digitally Verifiable</span><h2 id="billingDocumentTitle">TAX INVOICE</h2><label>Invoice No.<input value="${esc(editing?.invoice_number||"Generated on save")}" readonly></label><label>Issue date<input name="issue_date" type="date" value="${editing?.issue_date||today}" required></label><label>Due date<input name="due_date" type="date" value="${editing?.due_date||""}"></label></div></header><section class="hp-paper-section"><h3>BILL TO & PROJECT</h3><div class="hp-paper-details"><label>Client<input value="${esc(client.hospital_name||"")}" readonly></label><label>Client code<input value="${esc(client.client_code||"")}" readonly></label><label>Billing address<textarea readonly>${esc(client.billing_address||"")}</textarea></label><label>GSTIN<input value="${esc(client.gstin||"N/A")}" readonly></label><label>Project<input value="${esc(`${state.project.project_code} · ${state.project.title}`)}" readonly></label><label>Place of supply<input name="place_of_supply" value="${esc(editing?.place_of_supply||state.project.location||"")}"></label><label>Invoice type<select name="invoice_type">${["advance","progress","retention","final","other"].map(v=>`<option value="${v}" ${(editing?.invoice_type||"progress")===v?"selected":""}>${label(v)}</option>`).join("")}</select></label><label>Client reference / PO<input name="reference_number" value="${esc(editing?.reference_number||"")}"></label></div></section><section class="hp-paper-section hp-invoice-gst-settings"><h3>GST APPLICATION</h3><div><label>Apply GST<select name="gst_mode"><option value="item_wise" ${gstMode==="item_wise"?"selected":""}>Separately for each item</option><option value="common" ${gstMode==="common"?"selected":""}>Once on the entire bill</option></select></label><label data-common-gst ${gstMode==="common"?"":'hidden'}>Bill GST %<input name="common_gst_rate" type="number" min="0" max="100" step=".001" value="${commonRate}"></label><label data-common-gst ${gstMode==="common"?"":'hidden'}>GST treatment<select name="common_gst_included"><option value="false" ${commonIncluded?"":"selected"}>GST extra</option><option value="true" ${commonIncluded?"selected":""}>GST included</option></select></label></div><p>Choose one method only. Common bill GST overrides the GST fields on every item.</p></section><section class="hp-paper-section hp-paper-lines"><table><thead><tr><th>#</th><th>Item / description</th><th>HSN/SAC</th><th>Qty / Unit</th><th>Unit price</th><th>Discount</th><th data-billing-gst-head>GST</th><th data-billing-line-total-head>Total inc. GST</th><th></th></tr></thead><tbody id="hospitalBillingLines">${editLines.map(row).join("")}</tbody></table><button class="hp-paper-add" id="addBillingLine" type="button">+ Add invoice line</button></section><section class="hp-paper-commercial"><div class="hp-paper-terms"><label>Payment terms<textarea name="payment_terms" placeholder="Payment schedule and conditions">${esc(editing?.payment_terms||"")}</textarea></label><label>Notes<textarea name="notes" placeholder="Delivery, warranty or other notes">${esc(editing?.notes||"")}</textarea></label></div><div class="hp-paper-totals"><label>Round off<input name="round_off" type="number" step=".01" value="${Number(editing?.round_off||0)}"></label><span>Taxable value<strong id="billingTaxable">₹0</strong></span><span>GST<strong id="billingGst">₹0</strong></span><span class="grand">GRAND TOTAL<strong id="billingGrandTotal">₹0</strong></span></div></section><footer class="hp-paper-signature"><div class="hp-paper-authorisation"><span>For Varada Nexus Private Limited</span><div class="hp-paper-stamp"><img src="/new-ems/assets/pdf/vn-stamp.png" alt="Company stamp"><img src="/new-ems/assets/pdf/vn-signature.png" alt="Authorised signature"></div><strong>Authorised Signatory</strong></div></footer><div class="hp-paper-actions"><button class="btn" name="save_action" value="issue">Issue Invoice</button>${editing?`<button class="btn" id="cancelInvoiceEdit" type="button">Cancel edit</button>`:""}</div></form></div></div>`;
}

function billingPageV2() {
  const invoiceRows=state.invoices.map((x)=>{const doc=state.documents.find((d)=>d.entity_type==="hospital_invoices"&&d.entity_id===x.id),editable=x.status==="draft"&&x.accounts_status==="unposted"&&!Number(x.paid_amount)&&!Number(x.credited_amount);return `<tr><td><strong>${esc(x.invoice_number)}</strong><br><small>${esc(label(x.invoice_type))}</small></td><td>${date(x.issue_date)}<br><small>Due ${date(x.due_date)}</small></td><td>${money(x.total_amount)}</td><td>${money(x.paid_amount)}</td><td>${money(x.credited_amount)}</td><td>${money(x.balance_amount)}</td><td>${esc(label(x.status))}<br><small>Accounts: ${esc(label(x.accounts_status))}</small></td><td><div class="hp-actions"><button class="btn btn-sm" type="button" data-view-invoice="${x.id}">View</button><button class="btn btn-sm" type="button" data-download-invoice="${x.id}">PDF</button>${editable&&can(PERMISSIONS.EDIT)?`<button class="btn btn-sm" type="button" data-edit-invoice="${x.id}">Edit</button>`:""}${editable&&can(PERMISSIONS.DELETE)?`<button class="btn btn-sm" type="button" data-delete-invoice="${x.id}">Delete</button>`:""}${Number(x.balance_amount)>0&&can(PERMISSIONS.CREATE)?`<button class="btn btn-sm" type="button" data-new-credit="${x.id}">Credit note</button>`:""}</div></td></tr>`}).join("");
  const creditRows=state.credits.map((x)=>{const doc=state.documents.find((d)=>d.entity_type==="hospital_credit_notes"&&d.entity_id===x.id),editable=x.status==="draft"&&x.accounts_status==="unposted";return `<tr><td><strong>${esc(x.credit_note_number)}</strong></td><td>${esc(x.hospital_invoices?.invoice_number||"—")}</td><td>${date(x.credit_note_date)}</td><td>${esc(x.reason)}</td><td>${money(x.amount)}</td><td>${esc(label(x.status))}<br><small>Accounts: ${esc(label(x.accounts_status))}</small></td><td><div class="hp-actions"><button class="btn btn-sm" type="button" data-view-credit="${x.id}">View</button><button class="btn btn-sm" type="button" data-download-credit="${x.id}">PDF</button>${editable&&can(PERMISSIONS.EDIT)?`<button class="btn btn-sm" type="button" data-edit-credit="${x.id}">Edit</button>`:""}${editable&&can(PERMISSIONS.DELETE)?`<button class="btn btn-sm" type="button" data-delete-credit="${x.id}">Delete</button>`:""}</div></td></tr>`}).join("");
  return `${projectHeader()}<section class="hp-card hp-billing-launch"><div><span class="hp-eyebrow">PROJECT BILLING</span><h3>Client financial documents</h3><p>Create centrally numbered invoices and credit notes. The editor opens in the same visual order as the generated PDF.</p></div><div class="hp-actions">${can(PERMISSIONS.CREATE)?`<button class="btn" id="openBillGenerator" type="button">Generate Invoice</button><button class="btn" id="openCreditGenerator" type="button">Generate Credit Note</button>`:""}</div></section><section class="hp-card"><h3>Invoices</h3><p class="muted">Issued invoices are read by Central Accounts. Only unpaid, unposted drafts can be edited or hard-deleted.</p>${table(["Invoice","Issue / due","Total","Paid","Credited","Balance","Status","Actions"],invoiceRows,"No client invoices generated")}</section><section class="hp-card"><h3>Credit notes</h3><p class="muted">Credit notes use the centralized sequence and are stored in this project's Credit Notes folder.</p>${table(["Credit note","Against invoice","Date","Reason","Total","Status","Actions"],creditRows,"No credit notes issued")}</section>${billingInvoiceModalV2()}${billingCreditModal()}${billingPdfPreviewModal()}`;
}

function vendorPaymentsPage() {
  const today = new Date().toISOString().slice(0, 10);
  const assignedVendorIds = new Set(state.assignments.map((x) => x.vendor_id));
  const vendorOptions = state.vendors.filter((x) => assignedVendorIds.has(x.id)).map((x) => `<option value="${x.id}">${esc(x.vendor_code)} · ${esc(x.legal_name)}</option>`).join("");
  const modal = state.vendorBillModal ? `<div class="hp-communication-modal hp-vendor-bill-modal" role="dialog" aria-modal="true" aria-labelledby="vendorInvoiceModalTitle"><section class="hp-card hp-communication-dialog"><div class="hp-section-heading hp-actions"><div><span class="hp-eyebrow">VENDOR INVOICE INTAKE</span><h3 id="vendorInvoiceModalTitle">Upload vendor invoice</h3><p>The file is stored in this project's Vendor Bills folder and appears in the shared vendor-bill register.</p></div><button class="btn btn-sm" type="button" data-close-vendor-bill-modal>Close</button></div><form id="projectVendorBillForm" class="hp-form"><label>Assigned vendor<select name="vendor_id" required><option value="">Select vendor</option>${vendorOptions}</select></label><label>Vendor invoice number<input name="vendor_invoice_number" maxlength="120" required placeholder="Number printed on the vendor invoice"></label><label>Bill date<input name="bill_date" type="date" value="${today}" required></label><label>Due date<input name="due_date" type="date"></label><label>Taxable amount<input name="taxable_amount" type="number" min=".01" step=".01" required></label><label>GST rate %<input name="tax_rate" type="number" min="0" max="100" step=".001" value="18" required></label><label class="wide">Bill description<textarea name="description" rows="4" maxlength="2000" required placeholder="Items, work package or service covered by this invoice"></textarea></label><label class="wide">Invoice file<input name="invoice_file" type="file" accept="application/pdf,image/jpeg,image/png" required><small>PDF, JPG or PNG · maximum 10 MB</small></label><div class="hp-actions wide"><button class="btn" type="submit" ${vendorOptions ? "" : "disabled"}>Upload Invoice</button><button class="btn" type="button" data-close-vendor-bill-modal>Cancel</button></div></form>${vendorOptions ? "" : `<p class="muted">Assign a vendor to this project before uploading an invoice.</p>`}</section></div>` : "";
  const rows = state.vendorBills.map((x) => `<tr><td><strong>${esc(x.bill_number)}</strong><br><small>${esc(label(x.source || "staff"))}</small></td><td>${esc(x.vendor_invoice_number || "—")}</td><td>${esc(x.vendor_name)}</td><td>${date(x.bill_date)}<br><small>Due ${date(x.due_date)}</small></td><td>${money(x.total_amount)}</td><td>${money(x.paid_amount)}</td><td>${money(x.balance_amount)}</td><td>${esc(label(x.status))}</td><td>${x.web_view_link ? `<a class="btn btn-sm" href="${esc(x.web_view_link)}" target="_blank" rel="noopener">View invoice</a>` : "—"}</td></tr>`).join("");
  return `${projectHeader()}<section class="hp-card hp-billing-launch"><div><span class="hp-eyebrow">VENDOR INVOICE INTAKE</span><h3>Vendor bills</h3><p>Invoices submitted through the Hospital Vendor Portal appear here automatically. Staff can also upload an invoice for an assigned vendor.</p></div>${can(PERMISSIONS.CREATE) ? `<div class="hp-actions"><button class="btn" id="openVendorBillUpload" type="button" ${vendorOptions ? "" : "disabled"}>Upload Invoice</button></div>` : ""}</section><section class="hp-card"><h3>Project vendor bills and payment status</h3><p class="muted">Vendor-portal and EMS uploads share this register. Payment updates continue through the Hospital ledger and Central Accounts workflow.</p>${table(["Bill", "Vendor reference", "Vendor", "Bill / due", "Total", "Paid", "Balance", "Status", "Invoice file"], rows, "No vendor invoices uploaded")}</section>${modal}`;
}
function communicationModal() {
  const contact=projectContact(),subject=`${state.project.project_code} - ${state.project.title}`;
  if(state.communicationModal==="email")return `<div class="hp-communication-modal" role="dialog" aria-modal="true" aria-labelledby="hospitalEmailTitle"><section class="hp-card hp-communication-dialog"><div class="hp-section-heading hp-actions"><div><span class="hp-eyebrow">PROJECT EMAIL</span><h3 id="hospitalEmailTitle">Send email to ${esc(contact.name)}</h3><p>${esc(contact.email||"No email saved")}</p></div><button class="btn btn-sm" type="button" data-close-communication-modal>Close</button></div>${contact.email?`<form id="projectEmailForm" class="hp-form"><label class="wide">To<input name="to" type="email" value="${esc(contact.email)}" readonly></label><label class="wide">Subject<input name="subject" maxlength="200" value="${esc(subject)}" required></label><label class="wide">Message<textarea name="message" rows="9" required placeholder="Write the project update or message..."></textarea></label><div class="hp-actions wide"><button class="btn" type="submit">Send email</button><button class="btn" type="button" data-close-communication-modal>Cancel</button></div></form>`:`<div class="hp-empty">Add an email address to the Hospital / Client master before sending an email.</div>`}</section></div>`;
  if(state.communicationModal==="whatsapp")return `<div class="hp-communication-modal" role="dialog" aria-modal="true" aria-labelledby="hospitalWhatsappTitle"><section class="hp-card hp-communication-dialog hp-whatsapp-dialog"><div class="hp-section-heading hp-actions"><div><span class="hp-eyebrow">PROJECT WHATSAPP</span><h3 id="hospitalWhatsappTitle">${esc(contact.name)}</h3><p>${esc(contact.phone||"No WhatsApp number saved")}</p></div><button class="btn btn-sm" type="button" data-close-communication-modal>Close</button></div>${contact.phone?`<div class="hp-whatsapp-stream" id="hospitalWhatsappStream">${state.whatsappContactHistory.length?state.whatsappContactHistory.map((message)=>{const outbound=String(message.direction||"").toLowerCase()==="outbound";return `<article class="hp-whatsapp-message ${outbound?"outbound":"inbound"}"><small>${outbound?"EMS":esc(message.name||contact.name)} · ${esc(label(message.status))}</small><p>${esc(message.message||message.message_text||"")}</p><time>${esc(message.created_at?new Date(message.created_at).toLocaleString("en-IN"):"—")}</time></article>`;}).join(""):`<div class="hp-empty">No WhatsApp messages with this contact yet.</div>`}</div><form id="projectWhatsAppForm" class="hp-whatsapp-compose"><textarea name="message" rows="3" maxlength="1600" required placeholder="Type a WhatsApp message..."></textarea><button class="btn" type="submit">Send message</button></form><p class="muted hp-fine-print">Messages use the shared EMS WhatsApp service. Outside WhatsApp's active service window, an approved template may be required.</p>`:`<div class="hp-empty">Add a WhatsApp or mobile number to the Hospital / Client master before opening chat.</div>`}</section></div>`;
  if(state.communicationModal==="query")return `<div class="hp-communication-modal" role="dialog" aria-modal="true" aria-labelledby="hospitalQueryTitle"><section class="hp-card hp-communication-dialog"><div class="hp-section-heading hp-actions"><div><span class="hp-eyebrow">PROJECT QUERY DESK</span><h3 id="hospitalQueryTitle">Raise a query with ${esc(contact.name)}</h3><p>The query and its replies will be available in the Hospital Client Portal.</p></div><button class="btn btn-sm" type="button" data-close-communication-modal>Close</button></div><form id="projectQueryForm" class="hp-form"><label class="span2">Subject<input name="subject" maxlength="300" required placeholder="What needs the client's attention?"></label><label>Priority<select name="priority"><option value="normal">Normal</option><option value="high">High</option><option value="critical">Critical</option><option value="low">Low</option></select></label><label>Category<select name="category"><option value="general">General</option><option value="design">Design</option><option value="site">Site</option><option value="commercial">Commercial</option><option value="billing">Billing</option><option value="approval">Approval</option><option value="quality">Quality</option><option value="safety">Safety</option><option value="equipment">Equipment</option><option value="compliance">Compliance</option></select></label><label class="wide">Message<textarea name="body" rows="8" maxlength="10000" required placeholder="Describe the query clearly..."></textarea></label><div class="hp-actions wide"><button class="btn" type="submit">Raise query</button><button class="btn" type="button" data-close-communication-modal>Cancel</button></div></form></section></div>`;
  return "";
}

function communicationsPage() { const serviceHistory=[...state.emails.map(x=>({channel:"Email",recipient:x.to_email,subject:x.subject,status:x.status,at:x.created_at})),...state.whatsapp.map(x=>({channel:"WhatsApp",recipient:x.phone,subject:x.message||x.message_text||x.template_alias,status:x.status,at:x.created_at}))],contact=projectContact();return `${projectHeader()}<section class="hp-card"><h3>Project communications</h3><p>Send project-scoped Email, open the linked client's WhatsApp conversation, or raise a portal-tracked query without leaving this workspace.</p><div class="hp-workspace-links"><button class="hp-workspace-link" id="openProjectEmail" type="button"><strong>Send Email</strong><small>${esc(contact.email||"No client email saved")}</small></button><button class="hp-workspace-link" id="openProjectWhatsApp" type="button"><strong>WhatsApp Chat</strong><small>${esc(contact.phone||"No client WhatsApp number saved")}</small></button><button class="hp-workspace-link" id="openProjectQuery" type="button"><strong>Raise Query</strong><small>Send to the Hospital Client Portal</small></button></div></section><section class="hp-card"><h3>Project Query Desk</h3>${table(["Query", "Audience", "Status", "Messages", "Last activity"], state.queries.map((x) => `<tr><td><strong>${esc(x.subject)}</strong><br><small>${esc(x.query_number)}</small></td><td>${esc(label(x.audience))}</td><td>${esc(label(x.status))}</td><td>${state.messages.filter((m) => m.query_id === x.id).length}</td><td>${date(x.last_message_at)}</td></tr>`).join(""), "No project queries")}</section><section class="hp-card"><h3>Email and WhatsApp history</h3>${table(["Channel","Recipient","Message / subject","Status","Sent"],serviceHistory.map(x=>`<tr><td>${esc(x.channel)}</td><td>${esc(x.recipient||"—")}</td><td>${esc(x.subject||"—")}</td><td>${esc(label(x.status))}</td><td>${date(x.at)}</td></tr>`).join(""),"No tagged Email or WhatsApp history")}</section>${communicationModal()}`; }
function reportsPage() { const packageMargin=state.packages.reduce((a,x)=>a+Number(x.margin_amount||0),0),equipmentMargin=state.procurementPackages.reduce((a,x)=>a+Number(x.margin_amount||0),0),licenseMargin=state.licenses.reduce((a,x)=>a+Number(x.margin_amount||0),0),billed=state.invoices.reduce((a,x)=>a+Number(x.total_amount||0),0),outstanding=state.invoices.reduce((a,x)=>a+Number(x.balance_amount||0),0);return `${projectHeader()}<div class="hp-kpis">${kpi("Contract value",money(state.project.contract_value),"Project master")}${kpi("Billed",money(billed),`${money(outstanding)} outstanding`)}${kpi("Planned margin",money(packageMargin+equipmentMargin+licenseMargin),"Internal commercial view")}${kpi("Progress",`${Number(state.project.progress_percent||0)}%`,date(state.project.target_date))}</div><section class="hp-card"><h3>Margin by service</h3>${table(["Service area","Planned margin"],[['Work packages',packageMargin],['Equipment procurement packages',equipmentMargin],['Licensing & compliance',licenseMargin]].map(([name,value])=>`<tr><td>${name}</td><td>${money(value)}</td></tr>`).join(""))}<div class="hp-actions" style="margin-top:1rem"><a class="btn" href="${ROUTES.CENTRAL_ACCOUNTS_REPORTING}?source_module=hospital-projects&project_id=${encodeURIComponent(projectId)}">Open Central Accounts reporting</a></div></section>`; }
function settingsPage() { const scopeOptions=Object.entries(SERVICE_SCOPES).map(([code,name])=>`<label class="hp-scope-option"><input type="checkbox" name="scope_codes" value="${code}" ${state.scopes.includes(code)?"checked":""}><span>${esc(name)}</span></label>`).join("");return `${projectHeader()}<section class="hp-card"><h3>Project Settings</h3>${can(PERMISSIONS.EDIT)?`<form id="settingsForm" class="hp-form"><label>Status<select name="status">${["planning","design","procurement","execution","commissioning","completed","on_hold","cancelled"].map(x=>`<option value="${x}" ${state.project.status===x?"selected":""}>${label(x)}</option>`).join("")}</select></label><label>Priority<select name="priority">${["low","normal","high","critical"].map(x=>`<option value="${x}" ${state.project.priority===x?"selected":""}>${x}</option>`).join("")}</select></label><label>Progress %<input name="progress_percent" type="number" min="0" max="100" step=".01" value="${Number(state.project.progress_percent||0)}"></label><label>Start date<input name="start_date" type="date" value="${state.project.start_date||""}"></label><label>Target date<input name="target_date" type="date" value="${state.project.target_date||""}"></label><label class="wide">Scope summary<textarea name="scope_summary">${esc(state.project.scope_summary||"")}</textarea></label><fieldset class="wide"><legend>Service scopes</legend><div class="hp-scope-options">${scopeOptions}</div></fieldset><button class="btn">Save project settings</button></form>`:"<p>You have read-only access to this project.</p>"}</section>`; }

function body() { if(state.view==="overview")return overview();if(state.view==="planning")return packagesPage("planning");if(state.view==="construction")return packagesPage("construction");if(state.view==="procurement")return procurementWizardPage("Procurement");if(state.view==="medical-equipment")return procurementWizardPage("Medical Equipment");if(state.view==="licensing")return licensingPage();if(state.view==="contractors")return contractorsPage();if(state.view==="documents")return documentsPage();if(state.view==="quotations")return quotationsPage();if(state.view==="client-payments")return billingPageV2();if(state.view==="vendor-payments")return vendorPaymentsPage();if(state.view==="communications")return communicationsPage();if(state.view==="reports")return reportsPage();return settingsPage(); }

function values(form) { return Object.fromEntries([...new FormData(form)].map(([key,value]) => [key, typeof value === "string" && !value.trim() ? null : value])); }
function readBase64(file) { return new Promise((resolve,reject) => { const reader=new FileReader();reader.onload=()=>resolve(String(reader.result||"").split(",").pop()||"");reader.onerror=()=>reject(reader.error||new Error("File read failed"));reader.readAsDataURL(file); }); }

async function ensureProcurementPackageDraft(packageId, askForConfirmation = true) {
  const latest=state.procurementApprovals.filter((row)=>row.package_id===packageId).sort((a,b)=>Number(b.revision_no)-Number(a.revision_no))[0];
  if(!["pending","approved"].includes(latest?.status))return true;
  if(askForConfirmation&&!confirm(`This package has a ${label(latest.status)} client revision. Continue and start a new draft revision?`))return false;
  const {error}=await db.rpc("hospital_reopen_procurement_package",{p_package_id:packageId});
  if(error)throw error;
  await logAuditEvent("hospital_procurement_package_reopened",{moduleCode:MODULES.HOSPITAL_PROJECTS,entityType:"hospital_procurement_packages",entityId:packageId,beforeData:{approval_id:latest.id,status:latest.status},afterData:{status:"draft"}});
  await loadView();
  return true;
}

function bind() {
  document.getElementById("openBillGenerator")?.addEventListener("click",()=>{state.billingEditingId=null;state.billingModal="invoice";render();});
  document.getElementById("openCreditGenerator")?.addEventListener("click",()=>{state.creditEditingId="new:";state.billingModal="credit";render();});
  document.querySelectorAll("[data-close-billing-modal]").forEach((button)=>button.addEventListener("click",()=>{state.billingModal=null;state.billingEditingId=null;state.creditEditingId=null;render();}));
  document.getElementById("openQuotationGenerator")?.addEventListener("click",()=>{state.quotationEditingId=null;state.quotationModal=true;render();});
  document.querySelectorAll("[data-close-quotation-modal]").forEach((button)=>button.addEventListener("click",()=>{state.quotationModal=false;state.quotationEditingId=null;render();}));
  document.getElementById("cancelQuotationEdit")?.addEventListener("click",()=>{state.quotationModal=false;state.quotationEditingId=null;render();});
  document.querySelectorAll("[data-edit-quotation]").forEach((button)=>button.addEventListener("click",()=>{state.quotationEditingId=button.dataset.editQuotation;state.quotationModal=true;render();}));
  const quotationForm=document.getElementById("projectQuotationForm"),quotationBody=document.getElementById("hospitalQuotationLines");
  const recalcQuotation=()=>{if(!quotationForm)return;let taxable=0,gst=0;quotationBody?.querySelectorAll(".hospital-quotation-line").forEach((row)=>{const n=(name)=>Number(row.querySelector(`[name="${name}"]`)?.value||0),qty=n("quantity"),price=n("unit_price"),discount=n("discount_amount"),rate=n("gst_rate"),included=row.querySelector('[name="gst_included"]')?.value==="true",entered=Math.max(0,qty*price-discount);const base=included&&rate?entered*100/(100+rate):entered,tax=included?entered-base:base*rate/100,lineTotal=base+tax;taxable+=base;gst+=tax;const cell=row.querySelector(".billing-line-total");if(cell)cell.textContent=money(lineTotal);});const roundOff=Number(quotationForm.elements.round_off?.value||0),total=taxable+gst+roundOff;const set=(id,value)=>{const el=document.getElementById(id);if(el)el.textContent=money(value)};set("quotationTaxable",taxable);set("quotationGst",gst);set("quotationGrandTotal",total);};
  quotationBody?.addEventListener("input",recalcQuotation);quotationForm?.addEventListener("input",recalcQuotation);recalcQuotation();
  document.getElementById("addQuotationLine")?.addEventListener("click",()=>{if(!quotationBody)return;const row=document.createElement("tr");row.className="hospital-quotation-line";row.innerHTML=`<td>${quotationBody.children.length+1}</td><td class="hp-billing-item-cell"><input name="item_name" required placeholder="Item / equipment name"><textarea name="description" placeholder="Description (shown below item in PDF)"></textarea></td><td><input name="hsn_sac" placeholder="HSN/SAC"></td><td><input name="quantity" type="number" min=".001" step=".001" value="1" required><input name="unit" value="Nos" required></td><td><input name="unit_price" type="number" min="0" step=".01" value="0" required></td><td><input name="discount_amount" type="number" min="0" step=".01" value="0" placeholder="Optional"></td><td><input name="gst_rate" type="number" min="0" max="100" step=".001" value="18"><select name="gst_included"><option value="false">Extra</option><option value="true">Included</option></select></td><td class="billing-line-total">₹0</td><td><button class="hp-paper-remove" type="button" data-remove-quotation-line aria-label="Remove line">×</button></td>`;quotationBody.append(row);recalcQuotation();});
  quotationBody?.addEventListener("click",(event)=>{const button=event.target.closest("[data-remove-quotation-line]");if(!button||quotationBody.children.length<=1)return;button.closest("tr")?.remove();recalcQuotation();});
  quotationForm?.addEventListener("submit",async(event)=>{event.preventDefault();try{const header=values(quotationForm),numericFields=new Set(["quantity","unit_price","discount_amount","gst_rate"]);const lines=[...quotationBody.querySelectorAll(".hospital-quotation-line")].map((row)=>Object.fromEntries(["item_name","description","hsn_sac","quantity","unit","unit_price","discount_amount","gst_rate","gst_included"].map((name)=>{const input=row.querySelector(`[name="${name}"]`);return [name,numericFields.has(name)?Number(input?.value||0):name==="gst_included"?input?.value==="true":String(input?.value||"").trim()];})));const {data,error}=await db.rpc("hospital_save_quotation",{p_quotation_id:header.quotation_id||null,p_project_id:projectId,p_quotation_date:header.quotation_date,p_valid_until:header.valid_until,p_subject:header.subject,p_place_of_supply:header.place_of_supply,p_terms_conditions:header.terms_conditions,p_notes:header.notes,p_round_off:Number(header.round_off||0),p_lines:lines,p_include_signature:header.include_signature==="true"});if(error)throw error;await logAuditEvent(header.quotation_id?"hospital_quotation_updated":"hospital_quotation_created",{moduleCode:MODULES.HOSPITAL_PROJECTS,entityType:"hospital_quotations",entityId:data.id,afterData:{quotation_number:data.quotation_number,status:data.status,total_amount:data.total_amount}});state.quotationModal=false;state.quotationEditingId=null;showToast(`${data.quotation_number} saved.`,TOAST_TYPES.SUCCESS);await loadView();render();}catch(error){showToast(error.message,TOAST_TYPES.ERROR);}});
  document.querySelectorAll("[data-download-quotation]").forEach((button)=>button.addEventListener("click",async()=>{try{const quotation=state.quotations.find((x)=>x.id===button.dataset.downloadQuotation);await downloadHospitalQuotationPdf(quotation,state.project,state.project.hospital_clients,state.quotationLines.filter((x)=>x.quotation_id===quotation.id));}catch(error){showToast(error.message,TOAST_TYPES.ERROR);}}));
  document.querySelectorAll("[data-delete-quotation]").forEach((button)=>button.addEventListener("click",async()=>{const quotationId=button.dataset.deleteQuotation,row=state.quotations.find((x)=>x.id===quotationId);if(!row||!confirm(`Permanently delete draft ${row.quotation_number}?`))return;try{const {error}=await db.rpc("hospital_delete_quotation",{p_quotation_id:quotationId});if(error)throw error;await logAuditEvent("hospital_quotation_deleted",{moduleCode:MODULES.HOSPITAL_PROJECTS,entityType:"hospital_quotations",entityId:quotationId,beforeData:row,afterData:{hard_deleted:true}});showToast(`${row.quotation_number} deleted.`,TOAST_TYPES.SUCCESS);await loadView();render();}catch(error){showToast(error.message,TOAST_TYPES.ERROR);}}));
  document.querySelectorAll("[data-quotation-status]").forEach((button)=>button.addEventListener("click",async()=>{const [quotationId,newStatus]=String(button.dataset.quotationStatus).split(":"),row=state.quotations.find((x)=>x.id===quotationId);if(!row)return;try{const {data,error}=await db.rpc("hospital_update_quotation_status",{p_quotation_id:quotationId,p_status:newStatus});if(error)throw error;await logAuditEvent("hospital_quotation_status_updated",{moduleCode:MODULES.HOSPITAL_PROJECTS,entityType:"hospital_quotations",entityId:quotationId,beforeData:{status:row.status},afterData:{status:newStatus}});showToast(`${data.quotation_number} marked ${QUOTATION_STATUS_LABEL[newStatus]||newStatus}.`,TOAST_TYPES.SUCCESS);await loadView();render();}catch(error){showToast(error.message,TOAST_TYPES.ERROR);}}));
  document.getElementById("openProjectEmail")?.addEventListener("click",()=>{state.communicationModal="email";render();});
  document.getElementById("openProjectWhatsApp")?.addEventListener("click",()=>{state.communicationModal="whatsapp";render();requestAnimationFrame(()=>{const stream=document.getElementById("hospitalWhatsappStream");if(stream)stream.scrollTop=stream.scrollHeight;});});
  document.getElementById("openProjectQuery")?.addEventListener("click",()=>{state.communicationModal="query";render();});
  document.querySelectorAll("[data-close-communication-modal]").forEach((button)=>button.addEventListener("click",()=>{state.communicationModal=null;render();}));
  document.querySelector(".hp-communication-modal")?.addEventListener("click",(event)=>{if(event.target===event.currentTarget){state.communicationModal=null;render();}});
  document.getElementById("projectEmailForm")?.addEventListener("submit",async(event)=>{event.preventDefault();const form=event.currentTarget,button=form.querySelector('button[type="submit"]'),payload=values(form),contact=projectContact();try{button.disabled=true;button.textContent="Sending…";const text=String(payload.message||"").trim();await sendModuleEmail({to:[{address:payload.to,name:contact.name}],subject:payload.subject,textBody:text,bodyHtml:`<p>${esc(text).replace(/\n/g,"<br>")}</p>`,sourceModule:"hospital-projects",sourceEvent:state.project.project_code});await logAuditEvent("hospital_project_email_sent",{moduleCode:MODULES.HOSPITAL_PROJECTS,entityType:"hospital_projects",entityId:projectId,afterData:{recipient:payload.to,subject:payload.subject}});showToast("Project email sent.",TOAST_TYPES.SUCCESS);state.communicationModal=null;await loadView();render();}catch(error){showToast(error.message||"Email could not be sent.",TOAST_TYPES.ERROR);button.disabled=false;button.textContent="Send email";}});
  document.getElementById("projectWhatsAppForm")?.addEventListener("submit",async(event)=>{event.preventDefault();const form=event.currentTarget,button=form.querySelector('button[type="submit"]'),message=String(form.elements.message.value||"").trim(),contact=projectContact();if(!message)return;try{button.disabled=true;button.textContent="Sending…";await sendWhatsAppWorkspaceMessage({name:contact.name,phone:contact.phone,message,sourceModule:"hospital-projects",sourceEvent:state.project.project_code});await logAuditEvent("hospital_project_whatsapp_sent",{moduleCode:MODULES.HOSPITAL_PROJECTS,entityType:"hospital_projects",entityId:projectId,afterData:{recipient:contact.phone}});showToast("WhatsApp message sent.",TOAST_TYPES.SUCCESS);await loadView();state.communicationModal="whatsapp";render();requestAnimationFrame(()=>{const stream=document.getElementById("hospitalWhatsappStream");if(stream)stream.scrollTop=stream.scrollHeight;});}catch(error){showToast(error.message||"WhatsApp message could not be sent.",TOAST_TYPES.ERROR);button.disabled=false;button.textContent="Send message";}});
  document.getElementById("projectQueryForm")?.addEventListener("submit",async(event)=>{event.preventDefault();const form=event.currentTarget,button=form.querySelector('button[type="submit"]'),payload=values(form);let createdQuery=null;try{button.disabled=true;button.textContent="Creating…";const {data:queryNumber,error:numberError}=await db.rpc("hospital_next_document_number",{p_document_type:"QUERY",p_date:new Date().toISOString().slice(0,10)});if(numberError)throw numberError;const {data,error}=await db.from("hospital_queries").insert({query_number:queryNumber,project_id:projectId,client_id:state.project.client_id,audience:"client",subject:payload.subject,category:payload.category,priority:payload.priority,status:"awaiting_client"}).select("*").single();if(error)throw error;createdQuery=data;const {error:messageError}=await db.from("hospital_query_messages").insert({query_id:data.id,sender_kind:"staff",sender_user_id:state.boot?.user?.id||null,sender_label:"Varada Nexus",body:payload.body});if(messageError){await db.from("hospital_queries").delete().eq("id",data.id);throw messageError;}await logAuditEvent("hospital_project_query_created",{moduleCode:MODULES.HOSPITAL_PROJECTS,entityType:"hospital_queries",entityId:data.id,afterData:{project_id:projectId,query_number:data.query_number,audience:"client"}});showToast(`${data.query_number} sent to the client portal.`,TOAST_TYPES.SUCCESS);state.communicationModal=null;await loadView();render();}catch(error){if(createdQuery?.id)await db.from("hospital_queries").delete().eq("id",createdQuery.id);showToast(error.message||"Query could not be created.",TOAST_TYPES.ERROR);button.disabled=false;button.textContent="Raise query";}});
  document.querySelectorAll("[data-procurement-step]").forEach((button) => button.addEventListener("click", () => { const target=Number(button.dataset.procurementStep);if(target>=3&&!state.activeProcurementPackageId){state.procurementWizardStep=2;showToast("Select a vendor package before continuing.",TOAST_TYPES.ERROR);}else state.procurementWizardStep=target;render(); }));
  document.querySelector("[data-procurement-next]")?.addEventListener("click", () => { if(state.procurementWizardStep===1&&!state.items.length)return showToast("Add at least one requirement before continuing.",TOAST_TYPES.ERROR);if(state.procurementWizardStep===2&&!state.activeProcurementPackageId)return showToast("Select a vendor package before continuing.",TOAST_TYPES.ERROR);state.procurementWizardStep=Math.min(6,state.procurementWizardStep+1);render(); });
  document.querySelector("[data-procurement-back]")?.addEventListener("click", () => { state.procurementWizardStep=Math.max(1,state.procurementWizardStep-1);render(); });
  document.querySelector("[data-procurement-skip]")?.addEventListener("click", () => { state.procurementWizardStep=Math.min(6,state.procurementWizardStep+1);render(); });
  document.getElementById("changeProcurementPackage")?.addEventListener("click", () => { state.activeProcurementPackageId=null;state.procurementWizardStep=2;render(); });
  document.getElementById("projectPackageForm")?.addEventListener("submit", async (event) => { event.preventDefault();try{const payload=values(event.currentTarget);payload.project_id=projectId;for(const key of ["vendor_cost","additional_cost","client_price"])payload[key]=Number(payload[key]||0);const {data,error}=await db.from("hospital_work_packages").insert(payload).select("id").single();if(error)throw error;await logAuditEvent("hospital_work_package_created",{moduleCode:MODULES.HOSPITAL_PROJECTS,entityType:"hospital_work_packages",entityId:data?.id,afterData:payload});showToast("Work package added.",TOAST_TYPES.SUCCESS);await loadView();render();}catch(error){showToast(error.message,TOAST_TYPES.ERROR);} });
  document.getElementById("projectEquipmentForm")?.addEventListener("submit", async (event) => { event.preventDefault();try{const payload=values(event.currentTarget),editingId=state.equipmentEditingId;payload.quantity=Number(payload.quantity||0);if(editingId){const before=state.items.find((x)=>x.id===editingId);const {data,error}=await db.from("hospital_procurement_items").update(payload).eq("id",editingId).eq("project_id",projectId).select("id").maybeSingle();if(error)throw error;if(!data)throw new Error("Procurement requirement was not found or cannot be edited.");await logAuditEvent("hospital_equipment_item_updated",{moduleCode:MODULES.HOSPITAL_PROJECTS,entityType:"hospital_procurement_items",entityId:editingId,beforeData:before,afterData:payload});state.equipmentEditingId=null;showToast("Procurement requirement updated.",TOAST_TYPES.SUCCESS);}else{payload.project_id=projectId;const {data,error}=await db.from("hospital_procurement_items").insert(payload).select("id").single();if(error)throw error;await logAuditEvent("hospital_equipment_item_created",{moduleCode:MODULES.HOSPITAL_PROJECTS,entityType:"hospital_procurement_items",entityId:data?.id,afterData:payload});showToast("Procurement requirement added.",TOAST_TYPES.SUCCESS);}await loadView();render();}catch(error){showToast(error.message,TOAST_TYPES.ERROR);} });
  document.getElementById("cancelEquipmentEdit")?.addEventListener("click", () => { state.equipmentEditingId=null;render(); });
  document.querySelectorAll("[data-edit-equipment]").forEach((button) => button.addEventListener("click", () => { state.equipmentEditingId=button.dataset.editEquipment;render();requestAnimationFrame(()=>document.getElementById("projectEquipmentForm")?.scrollIntoView({behavior:"smooth",block:"start"})); }));
  document.querySelectorAll("[data-delete-equipment]").forEach((button) => button.addEventListener("click", async () => { const item=state.items.find((x)=>x.id===button.dataset.deleteEquipment);if(!item||!confirm(`Delete equipment “${item.equipment_name}”? This cannot be undone.`))return;try{const {data,error}=await db.from("hospital_procurement_items").delete().eq("id",item.id).eq("project_id",projectId).select("id").maybeSingle();if(error)throw error;if(!data)throw new Error("Equipment record was not found or cannot be deleted.");await logAuditEvent("hospital_equipment_item_deleted",{moduleCode:MODULES.HOSPITAL_PROJECTS,entityType:"hospital_procurement_items",entityId:item.id,beforeData:item});if(state.equipmentEditingId===item.id)state.equipmentEditingId=null;showToast("Equipment item deleted.",TOAST_TYPES.SUCCESS);await loadView();render();}catch(error){showToast(error.message,TOAST_TYPES.ERROR);} }));
  document.getElementById("procurementPackageForm")?.addEventListener("submit", async (event) => { event.preventDefault();try{const form=event.currentTarget,formData=new FormData(form),itemIds=formData.getAll("procurement_item_ids");if(!itemIds.length)throw new Error("Select at least one procurement requirement.");const payload=values(form);const {data,error}=await db.rpc("hospital_create_procurement_package",{p_project_id:projectId,p_vendor_id:payload.vendor_id,p_procurement_item_ids:itemIds,p_vendor_quote_number:payload.vendor_quote_number,p_quote_date:payload.quote_date,p_valid_until:payload.valid_until,p_expected_delivery:payload.expected_delivery,p_place_of_supply:payload.place_of_supply,p_tax_mode:payload.tax_mode,p_common_gst_rate:Number(payload.common_gst_rate||0),p_notes:payload.notes});if(error)throw error;state.activeProcurementPackageId=data?.id;state.procurementWizardStep=3;await logAuditEvent("hospital_procurement_package_created",{moduleCode:MODULES.HOSPITAL_PROJECTS,entityType:"hospital_procurement_packages",entityId:data?.id,afterData:{project_id:projectId,package_number:data?.package_number,item_ids:itemIds}});showToast(`Procurement package ${data?.package_number||""} created.`,TOAST_TYPES.SUCCESS);await loadView();render();}catch(error){showToast(error.message,TOAST_TYPES.ERROR);} });
  document.querySelectorAll("[data-open-procurement-package]").forEach((button) => button.addEventListener("click", () => { state.activeProcurementPackageId=button.dataset.openProcurementPackage;state.procurementWizardStep=3;render(); }));
  document.querySelectorAll("[data-edit-procurement-package]").forEach((button) => button.addEventListener("click", () => { state.activeProcurementPackageId=button.dataset.editProcurementPackage;state.procurementPackageEditingId=button.dataset.editProcurementPackage;state.procurementWizardStep=2;render(); }));
  document.getElementById("cancelProcurementPackageEdit")?.addEventListener("click", () => { state.procurementPackageEditingId=null;render(); });
  document.getElementById("closeProcurementPackage")?.addEventListener("click", () => { state.activeProcurementPackageId=null;render(); });
  document.getElementById("procurementPackageHeaderForm")?.addEventListener("submit", async (event) => { event.preventDefault();try{const payload=values(event.currentTarget);for(const key of ["common_gst_rate","vendor_round_off","client_round_off"])payload[key]=Number(payload[key]||0);const before=state.procurementPackages.find((x)=>x.id===state.activeProcurementPackageId);const {data,error}=await db.from("hospital_procurement_packages").update(payload).eq("id",state.activeProcurementPackageId).eq("project_id",projectId).select("id").maybeSingle();if(error)throw error;if(!data)throw new Error("Procurement package cannot be updated.");await logAuditEvent("hospital_procurement_package_updated",{moduleCode:MODULES.HOSPITAL_PROJECTS,entityType:"hospital_procurement_packages",entityId:data.id,beforeData:before,afterData:payload});showToast("Procurement package updated.",TOAST_TYPES.SUCCESS);await loadView();render();}catch(error){showToast(error.message,TOAST_TYPES.ERROR);} });
  document.getElementById("addRequirementToPackageForm")?.addEventListener("submit", async (event) => { event.preventDefault();try{const select=event.currentTarget.elements.procurement_item_id,option=select.selectedOptions[0];if(!option?.value)throw new Error("Select a procurement requirement.");const payload={package_id:state.activeProcurementPackageId,procurement_item_id:option.value,quantity:Number(option.dataset.quantity||1),unit:option.dataset.unit||"Nos"};const {data,error}=await db.from("hospital_procurement_package_items").insert(payload).select("id").single();if(error)throw error;await logAuditEvent("hospital_procurement_package_item_created",{moduleCode:MODULES.HOSPITAL_PROJECTS,entityType:"hospital_procurement_package_items",entityId:data.id,afterData:payload});showToast("Requirement added to the package.",TOAST_TYPES.SUCCESS);await loadView();render();}catch(error){showToast(error.message,TOAST_TYPES.ERROR);} });
  document.querySelectorAll("[data-save-procurement-line]").forEach((button) => button.addEventListener("click", async () => { try{const row=button.closest("tr"),line=state.procurementLines.find((x)=>x.id===button.dataset.saveProcurementLine),payload={};for(const input of row.querySelectorAll("input,select"))payload[input.name]=input.value||null;for(const key of ["quantity","vendor_unit_price","vendor_discount","client_unit_price","client_discount","gst_rate"])payload[key]=key==="gst_rate"&&row.querySelector('[name="gst_rate"]')?.disabled?Number(line.gst_rate||0):Number(payload[key]||0);payload.gst_included=payload.gst_included==="true";const {data,error}=await db.from("hospital_procurement_package_items").update(payload).eq("id",line.id).eq("package_id",state.activeProcurementPackageId).select("id").maybeSingle();if(error)throw error;if(!data)throw new Error("Package item cannot be updated.");await logAuditEvent("hospital_procurement_package_item_updated",{moduleCode:MODULES.HOSPITAL_PROJECTS,entityType:"hospital_procurement_package_items",entityId:line.id,beforeData:line,afterData:payload});showToast("Package item pricing and GST treatment saved.",TOAST_TYPES.SUCCESS);await loadView();render();}catch(error){showToast(error.message,TOAST_TYPES.ERROR);} }));
  document.getElementById("procurementChargeForm")?.addEventListener("submit", async (event) => { event.preventDefault();try{const payload=values(event.currentTarget),editingId=state.procurementChargeEditingId,before=state.procurementCharges.find((row)=>row.id===editingId);payload.package_id=state.activeProcurementPackageId;for(const key of ["vendor_amount","client_amount","gst_rate"])payload[key]=key==="gst_rate"&&event.currentTarget.elements.gst_rate.disabled?Number(before?.gst_rate||0):Number(payload[key]||0);payload.gst_included=payload.gst_included==="true";const query=editingId?db.from("hospital_procurement_package_charges").update(payload).eq("id",editingId).eq("package_id",state.activeProcurementPackageId):db.from("hospital_procurement_package_charges").insert(payload);const {data,error}=await query.select("id").maybeSingle();if(error)throw error;if(!data)throw new Error("Package charge cannot be saved.");await logAuditEvent(editingId?"hospital_procurement_package_charge_updated":"hospital_procurement_package_charge_created",{moduleCode:MODULES.HOSPITAL_PROJECTS,entityType:"hospital_procurement_package_charges",entityId:data.id,beforeData:before,afterData:payload});state.procurementChargeEditingId=null;showToast(editingId?"Package charge updated.":"Package-level charge added.",TOAST_TYPES.SUCCESS);await loadView();render();}catch(error){showToast(error.message,TOAST_TYPES.ERROR);} });
  document.getElementById("cancelProcurementChargeEdit")?.addEventListener("click",()=>{state.procurementChargeEditingId=null;render();});
  document.querySelectorAll("[data-edit-procurement-charge]").forEach((button)=>button.addEventListener("click",async()=>{try{if(!await ensureProcurementPackageDraft(state.activeProcurementPackageId))return;state.procurementChargeEditingId=button.dataset.editProcurementCharge;render();requestAnimationFrame(()=>document.getElementById("procurementChargeForm")?.scrollIntoView({behavior:"smooth",block:"start"}));}catch(error){showToast(error.message,TOAST_TYPES.ERROR);}}));
  document.getElementById("procurementTaxForm")?.addEventListener("submit", async (event) => { event.preventDefault();try{const payload=values(event.currentTarget);payload.package_id=state.activeProcurementPackageId;for(const key of ["taxable_base","tax_rate","tax_amount"])payload[key]=Number(payload[key]||0);if(!payload.tax_amount&&payload.taxable_base&&payload.tax_rate)payload.tax_amount=Math.round(payload.taxable_base*payload.tax_rate)/100;const {data,error}=await db.from("hospital_procurement_package_taxes").insert(payload).select("id").single();if(error)throw error;await logAuditEvent("hospital_procurement_package_tax_created",{moduleCode:MODULES.HOSPITAL_PROJECTS,entityType:"hospital_procurement_package_taxes",entityId:data.id,afterData:payload});showToast("Tax or deduction added.",TOAST_TYPES.SUCCESS);await loadView();render();}catch(error){showToast(error.message,TOAST_TYPES.ERROR);} });
  document.querySelectorAll("[data-delete-procurement-line],[data-delete-procurement-charge],[data-delete-procurement-tax]").forEach((button) => button.addEventListener("click", async () => { const config=button.dataset.deleteProcurementLine?["hospital_procurement_package_items",button.dataset.deleteProcurementLine,"package item"]:button.dataset.deleteProcurementCharge?["hospital_procurement_package_charges",button.dataset.deleteProcurementCharge,"package charge"]:["hospital_procurement_package_taxes",button.dataset.deleteProcurementTax,"tax or deduction"];if(!confirm(`Remove this ${config[2]}?`))return;try{if(button.dataset.deleteProcurementCharge&&!await ensureProcurementPackageDraft(state.activeProcurementPackageId))return;const before=button.dataset.deleteProcurementCharge?state.procurementCharges.find((row)=>row.id===config[1]):null;const {data,error}=await db.from(config[0]).delete().eq("id",config[1]).eq("package_id",state.activeProcurementPackageId).select("id").maybeSingle();if(error)throw error;if(!data)throw new Error(`The ${config[2]} cannot be removed.`);await logAuditEvent("hospital_procurement_package_component_deleted",{moduleCode:MODULES.HOSPITAL_PROJECTS,entityType:config[0],entityId:config[1],beforeData:before});if(state.procurementChargeEditingId===config[1])state.procurementChargeEditingId=null;showToast(`${label(config[2])} removed.`,TOAST_TYPES.SUCCESS);await loadView();render();}catch(error){showToast(error.message,TOAST_TYPES.ERROR);} }));
  document.querySelectorAll("[data-delete-procurement-package]").forEach((button) => button.addEventListener("click", async () => { const pkg=state.procurementPackages.find((x)=>x.id===button.dataset.deleteProcurementPackage);if(!pkg||!confirm(`Delete draft package ${pkg.package_number} and all its pricing?`))return;try{const {data,error}=await db.from("hospital_procurement_packages").delete().eq("id",pkg.id).eq("project_id",projectId).eq("status","draft").select("id").maybeSingle();if(error)throw error;if(!data)throw new Error("Only a draft procurement package can be deleted.");await logAuditEvent("hospital_procurement_package_deleted",{moduleCode:MODULES.HOSPITAL_PROJECTS,entityType:"hospital_procurement_packages",entityId:pkg.id,beforeData:pkg});if(state.activeProcurementPackageId===pkg.id)state.activeProcurementPackageId=null;showToast("Draft procurement package deleted.",TOAST_TYPES.SUCCESS);await loadView();render();}catch(error){showToast(error.message,TOAST_TYPES.ERROR);} }));
  document.querySelector("[data-edit-procurement-terms]")?.addEventListener("click", async () => { const active=state.procurementPackages.find((row)=>row.id===state.activeProcurementPackageId);if(!active)return;const terms=procurementTermsFor(active,state.procurementApprovals.filter((row)=>row.package_id===active.id).sort((a,b)=>Number(b.revision_no)-Number(a.revision_no))[0]||null);try{if(!await ensureProcurementPackageDraft(active.id))return;state.procurementTermsByPackage[active.id]=terms;showToast("Terms opened for editing in a new draft revision.",TOAST_TYPES.SUCCESS);render();}catch(error){showToast(error.message,TOAST_TYPES.ERROR);} });
  document.querySelector("[data-delete-procurement-terms]")?.addEventListener("click", async () => { const active=state.procurementPackages.find((row)=>row.id===state.activeProcurementPackageId);if(!active||!confirm("Delete all terms and conditions from the next client proposal revision?"))return;try{if(!await ensureProcurementPackageDraft(active.id,false))return;const {data,error}=await db.from("hospital_procurement_packages").update({client_terms:null}).eq("id",active.id).eq("project_id",projectId).select("id").maybeSingle();if(error)throw error;if(!data)throw new Error("The procurement terms could not be deleted.");state.procurementTermsByPackage[active.id]="";await logAuditEvent("hospital_procurement_terms_deleted",{moduleCode:MODULES.HOSPITAL_PROJECTS,entityType:"hospital_procurement_packages",entityId:active.id,beforeData:{client_terms:active.client_terms},afterData:{client_terms:null}});await loadView();showToast("Terms and conditions deleted from the draft proposal.",TOAST_TYPES.SUCCESS);render();}catch(error){showToast(error.message,TOAST_TYPES.ERROR);} });
  document.getElementById("procurementTermsForm")?.addEventListener("submit", async (event) => { event.preventDefault();const active=state.procurementPackages.find((row)=>row.id===state.activeProcurementPackageId);if(!active)return showToast("Select a procurement package.",TOAST_TYPES.ERROR);const terms=composeProcurementTerms(values(event.currentTarget));if(!terms)return showToast("Enter the proposal terms or use Skip for now.",TOAST_TYPES.ERROR);try{const {data,error}=await db.from("hospital_procurement_packages").update({client_terms:terms}).eq("id",active.id).eq("project_id",projectId).select("id").maybeSingle();if(error)throw error;if(!data)throw new Error("The procurement terms could not be saved.");state.procurementTermsByPackage[active.id]=terms;await logAuditEvent("hospital_procurement_terms_saved",{moduleCode:MODULES.HOSPITAL_PROJECTS,entityType:"hospital_procurement_packages",entityId:active.id,beforeData:{client_terms:active.client_terms},afterData:{client_terms:terms}});await loadView();state.procurementWizardStep=6;showToast("Client terms saved for this proposal draft.",TOAST_TYPES.SUCCESS);render();}catch(error){showToast(error.message,TOAST_TYPES.ERROR);} });
  document.getElementById("procurementApprovalForm")?.addEventListener("submit", async (event) => { event.preventDefault();try{const active=state.procurementPackages.find((row)=>row.id===state.activeProcurementPackageId);if(!active)throw new Error("Select a procurement package.");const latest=state.procurementApprovals.filter((row)=>row.package_id===active.id).sort((a,b)=>Number(b.revision_no)-Number(a.revision_no))[0]||null;const terms=procurementTermsFor(active,latest);const {data,error}=await db.rpc("hospital_submit_procurement_for_approval",{p_package_id:active.id,p_terms:terms||null});if(error)throw error;await logAuditEvent("hospital_procurement_sent_for_client_approval",{moduleCode:MODULES.HOSPITAL_PROJECTS,entityType:"hospital_procurement_approvals",entityId:data?.id,afterData:data});showToast(`${data?.approval_number||"Procurement proposal"} is available in the client portal.`,TOAST_TYPES.SUCCESS);await loadView();render();}catch(error){showToast(error.message,TOAST_TYPES.ERROR);} });
  document.getElementById("downloadProcurementProposal")?.addEventListener("click", async () => { try{const active=state.procurementPackages.find((row)=>row.id===state.activeProcurementPackageId);if(!active)throw new Error("Select a procurement package.");const latest=state.procurementApprovals.filter((row)=>row.package_id===active.id).sort((a,b)=>Number(b.revision_no)-Number(a.revision_no))[0]||null,terms=procurementTermsFor(active,latest),snapshot=["pending","approved"].includes(latest?.status)?latest.payload_snapshot:procurementClientSnapshot(active,terms);await downloadHospitalProcurementProposal(snapshot);}catch(error){showToast(error.message,TOAST_TYPES.ERROR);} });
  document.getElementById("createPurchaseOrder")?.addEventListener("click", async () => { try{const active=state.procurementPackages.find((row)=>row.id===state.activeProcurementPackageId);if(!active)throw new Error("Select a procurement package.");const {data,error}=await db.rpc("hospital_create_purchase_order",{p_package_id:active.id,p_po_date:new Date().toISOString().slice(0,10),p_terms:null});if(error)throw error;await logAuditEvent("hospital_purchase_order_created",{moduleCode:MODULES.HOSPITAL_PROJECTS,entityType:"hospital_purchase_orders",entityId:data?.id,afterData:data});await loadView();const po=state.purchaseOrders.find((row)=>row.package_id===active.id);if(po?.payload_snapshot)await downloadHospitalPurchaseOrder(po.payload_snapshot);showToast(`${data?.po_number||"Purchase order"} generated.`,TOAST_TYPES.SUCCESS);render();}catch(error){showToast(error.message,TOAST_TYPES.ERROR);} });
  document.getElementById("downloadPurchaseOrder")?.addEventListener("click", async () => { try{const po=state.purchaseOrders.find((row)=>row.package_id===state.activeProcurementPackageId);if(!po?.payload_snapshot)throw new Error("Purchase order snapshot is unavailable.");await downloadHospitalPurchaseOrder(po.payload_snapshot);}catch(error){showToast(error.message,TOAST_TYPES.ERROR);} });
  document.getElementById("projectLicenseForm")?.addEventListener("submit", async (event) => { event.preventDefault();try{const payload=values(event.currentTarget);payload.project_id=projectId;for(const key of ["statutory_fee","vendor_service_cost","client_price"])payload[key]=Number(payload[key]||0);const {data,error}=await db.from("hospital_license_requirements").insert(payload).select("id").single();if(error)throw error;await logAuditEvent("hospital_license_requirement_created",{moduleCode:MODULES.HOSPITAL_PROJECTS,entityType:"hospital_license_requirements",entityId:data?.id,afterData:payload});showToast("Licence requirement added.",TOAST_TYPES.SUCCESS);await loadView();render();}catch(error){showToast(error.message,TOAST_TYPES.ERROR);} });
  const billingForm=document.getElementById("projectInvoiceForm"),billingBody=document.getElementById("hospitalBillingLines");
  const billingGstMode=()=>billingForm?.elements.gst_mode?.value||"item_wise";
  const syncBillingGstMode=()=>{if(!billingForm)return;const common=billingGstMode()==="common";billingForm.querySelectorAll("[data-common-gst]").forEach((el)=>el.hidden=!common);billingBody?.querySelectorAll(".hospital-billing-line").forEach((row)=>{const item=row.querySelector(".hp-line-gst"),commonLabel=row.querySelector(".hp-common-gst-label");if(item)item.hidden=common;if(commonLabel)commonLabel.hidden=true;});const gstHead=billingForm.querySelector("[data-billing-gst-head]"),totalHead=billingForm.querySelector("[data-billing-line-total-head]");if(gstHead)gstHead.hidden=common;if(totalHead)totalHead.textContent=common?"Line total":"Total inc. GST";};
  const recalcBilling=()=>{if(!billingForm)return;const common=billingGstMode()==="common",commonRate=Number(billingForm.elements.common_gst_rate?.value||0),commonIncluded=billingForm.elements.common_gst_included?.value==="true";let enteredTotal=0,taxable=0,gst=0,total=0;billingBody?.querySelectorAll(".hospital-billing-line").forEach((row)=>{const n=(name)=>Number(row.querySelector(`[name="${name}"]`)?.value||0),qty=n("quantity"),price=n("unit_price"),discount=n("discount_amount"),rate=common?commonRate:n("gst_rate"),included=common?commonIncluded:row.querySelector('[name="gst_included"]')?.value==="true",entered=Math.max(0,qty*price-discount);enteredTotal+=entered;if(common){const cell=row.querySelector(".billing-line-total");if(cell)cell.textContent=money(entered);return;}const base=included&&rate?entered*100/(100+rate):entered,tax=included?entered-base:base*rate/100,lineTotal=base+tax;taxable+=base;gst+=tax;total+=lineTotal;const cell=row.querySelector(".billing-line-total");if(cell)cell.textContent=money(lineTotal);});if(common){taxable=commonIncluded&&commonRate?enteredTotal*100/(100+commonRate):enteredTotal;gst=commonIncluded?enteredTotal-taxable:taxable*commonRate/100;total=taxable+gst;}total+=Number(billingForm.elements.round_off?.value||0);const set=(id,value)=>{const el=document.getElementById(id);if(el)el.textContent=money(value)};set("billingTaxable",taxable);set("billingGst",gst);set("billingGrandTotal",total);};
  billingBody?.addEventListener("input",recalcBilling);billingForm?.addEventListener("input",recalcBilling);billingForm?.elements.gst_mode?.addEventListener("change",()=>{syncBillingGstMode();recalcBilling();});syncBillingGstMode();recalcBilling();
  document.getElementById("addBillingLine")?.addEventListener("click",()=>{if(!billingBody)return;const common=billingGstMode()==="common",row=document.createElement("tr");row.className="hospital-billing-line";row.innerHTML=`<td>${billingBody.children.length+1}</td><td class="hp-billing-item-cell"><input name="item_name" required placeholder="Item / service name"><textarea name="description" placeholder="Description (shown below item in PDF)"></textarea></td><td><input name="hsn_sac" placeholder="HSN/SAC"></td><td><input name="quantity" type="number" min=".001" step=".001" value="1" required><input name="unit" value="Nos" required></td><td><input name="unit_price" type="number" min="0" step=".01" value="0" required></td><td><input name="discount_amount" type="number" min="0" step=".01" value="0" placeholder="Optional"></td><td class="hp-line-gst" ${common?"hidden":""}><input name="gst_rate" type="number" min="0" max="100" step=".001" value="18"><select name="gst_included"><option value="false">Extra</option><option value="true">Included</option></select></td><td class="hp-common-gst-label" hidden>Common bill GST</td><td class="billing-line-total">₹0</td><td><button class="hp-paper-remove" type="button" data-remove-billing-line aria-label="Remove line">×</button></td>`;billingBody.append(row);syncBillingGstMode();recalcBilling();});
  billingBody?.addEventListener("click",(event)=>{const button=event.target.closest("[data-remove-billing-line]");if(!button||billingBody.children.length<=1)return;button.closest("tr")?.remove();recalcBilling();});
  billingForm?.addEventListener("submit",async(event)=>{event.preventDefault();try{const header=values(billingForm),numericFields=new Set(["quantity","unit_price","discount_amount","gst_rate"]),lines=[...billingBody.querySelectorAll(".hospital-billing-line")].map((row)=>Object.fromEntries(["item_name","description","hsn_sac","quantity","unit","unit_price","discount_amount","gst_rate","gst_included"].map((name)=>{const input=row.querySelector(`[name="${name}"]`);return [name,numericFields.has(name)?Number(input?.value||0):name==="gst_included"?input?.value==="true":String(input?.value||"").trim()]})));const issue=true,gstMode=header.gst_mode==="common"?"common":"item_wise";const {data,error}=await db.rpc("hospital_save_invoice",{p_invoice_id:header.invoice_id||null,p_project_id:projectId,p_invoice_type:header.invoice_type,p_issue_date:header.issue_date,p_due_date:header.due_date,p_reference_number:header.reference_number,p_place_of_supply:header.place_of_supply,p_payment_terms:header.payment_terms,p_notes:header.notes,p_round_off:Number(header.round_off||0),p_gst_mode:gstMode,p_common_gst_rate:Number(header.common_gst_rate||0),p_common_gst_included:header.common_gst_included==="true",p_lines:lines,p_issue:issue});if(error)throw error;const {data:savedLines,error:lineError}=await db.from("hospital_invoice_lines").select("*").eq("invoice_id",data.id).order("line_no");if(lineError)throw lineError;const saved={...data,entityType:"hospital_invoices"};const archived=await archiveHospitalBillingPdf(saved,state.project,state.project.hospital_clients,savedLines||[]);await logAuditEvent(header.invoice_id?"hospital_client_invoice_updated":"hospital_client_invoice_created",{moduleCode:MODULES.HOSPITAL_PROJECTS,entityType:"hospital_invoices",entityId:data.id,afterData:{invoice_number:data.invoice_number,status:data.status,gst_mode:gstMode,drive_file_id:archived.result.fileId}});state.billingEditingId=null;state.billingModal=null;showToast(`${data.invoice_number} saved to the Hospital Projects ledger and archived in the project Drive folder.`,TOAST_TYPES.SUCCESS);await loadView();render();}catch(error){showToast(error.message,TOAST_TYPES.ERROR);}});
  document.getElementById("cancelInvoiceEdit")?.addEventListener("click",()=>{state.billingEditingId=null;state.billingModal=null;render();});
  document.querySelectorAll("[data-edit-invoice]").forEach((button)=>button.addEventListener("click",()=>{state.billingEditingId=button.dataset.editInvoice;state.billingModal="invoice";render();}));
  document.querySelectorAll("[data-new-credit]").forEach((button)=>button.addEventListener("click",()=>{state.creditEditingId=`new:${button.dataset.newCredit}`;state.billingModal="credit";render();}));
  document.querySelectorAll("[data-edit-credit]").forEach((button)=>button.addEventListener("click",()=>{state.creditEditingId=button.dataset.editCredit;state.billingModal="credit";render();}));
  document.getElementById("cancelCreditEdit")?.addEventListener("click",()=>{state.creditEditingId=null;state.billingModal=null;render();});
  const creditForm=document.getElementById("creditNoteForm"),recalcCredit=()=>{if(!creditForm)return;const taxable=Number(creditForm.elements.taxable_amount?.value||0),rate=Number(creditForm.elements.tax_rate?.value||0),gst=taxable*rate/100;[["creditTaxable",taxable],["creditGst",gst],["creditGrandTotal",taxable+gst]].forEach(([id,value])=>{const el=document.getElementById(id);if(el)el.textContent=money(value);});};creditForm?.elements.taxable_amount?.addEventListener("input",recalcCredit);creditForm?.elements.tax_rate?.addEventListener("input",recalcCredit);recalcCredit();
  creditForm?.addEventListener("submit",async(event)=>{event.preventDefault();try{const payload=values(event.currentTarget),approve=true;const {data,error}=await db.rpc("hospital_save_credit_note",{p_credit_note_id:payload.credit_note_id||null,p_invoice_id:payload.invoice_id,p_credit_note_date:payload.credit_note_date,p_reason:payload.reason,p_taxable_amount:Number(payload.taxable_amount||0),p_tax_rate:Number(payload.tax_rate||0),p_notes:payload.notes,p_approve:approve});if(error)throw error;const saved={...data,entityType:"hospital_credit_notes"};const archived=await archiveHospitalBillingPdf(saved,state.project,state.project.hospital_clients,[]);await logAuditEvent(payload.credit_note_id?"hospital_credit_note_updated":"hospital_credit_note_created",{moduleCode:MODULES.HOSPITAL_PROJECTS,entityType:"hospital_credit_notes",entityId:data.id,afterData:{credit_note_number:data.credit_note_number,status:data.status,drive_file_id:archived.result.fileId}});state.creditEditingId=null;state.billingModal=null;showToast(`${data.credit_note_number} saved to the Hospital Projects ledger and archived in Credit Notes.`,TOAST_TYPES.SUCCESS);await loadView();render();}catch(error){showToast(error.message,TOAST_TYPES.ERROR);}});
  const openBillingPreview=async(documentRecord,lines=[])=>{try{const document={...documentRecord,entityType:documentRecord.credit_note_number?"hospital_credit_notes":"hospital_invoices"},pdf=await buildHospitalBillingPdf(document,state.project,state.project.hospital_clients,lines);if(state.billingPreview?.url)URL.revokeObjectURL(state.billingPreview.url);state.billingPreview={url:pdf.output("bloburl"),entityType:document.entityType,entityId:document.id,title:document.invoice_number||document.credit_note_number};render();}catch(error){showToast(error.message,TOAST_TYPES.ERROR);}};
  document.querySelectorAll("[data-view-invoice]").forEach((button)=>button.addEventListener("click",()=>{const invoice=state.invoices.find((x)=>x.id===button.dataset.viewInvoice);if(invoice)openBillingPreview(invoice,state.invoiceLines.filter((x)=>x.invoice_id===invoice.id));}));
  document.querySelectorAll("[data-view-credit]").forEach((button)=>button.addEventListener("click",()=>{const note=state.credits.find((x)=>x.id===button.dataset.viewCredit);if(note)openBillingPreview(note,[]);}));
  document.querySelector("[data-close-billing-preview]")?.addEventListener("click",()=>{if(state.billingPreview?.url)URL.revokeObjectURL(state.billingPreview.url);state.billingPreview=null;render();});
  document.getElementById("downloadBillingPreview")?.addEventListener("click",async()=>{const preview=state.billingPreview;if(!preview)return;const record=preview.entityType==="hospital_invoices"?state.invoices.find((x)=>x.id===preview.entityId):state.credits.find((x)=>x.id===preview.entityId);if(!record)return;await downloadHospitalBillingPdf({...record,entityType:preview.entityType},state.project,state.project.hospital_clients,preview.entityType==="hospital_invoices"?state.invoiceLines.filter((x)=>x.invoice_id===record.id):[]);});
  document.querySelectorAll("[data-download-invoice]").forEach((button)=>button.addEventListener("click",async()=>{try{const invoice=state.invoices.find((x)=>x.id===button.dataset.downloadInvoice);await downloadHospitalBillingPdf({...invoice,entityType:"hospital_invoices"},state.project,state.project.hospital_clients,state.invoiceLines.filter((x)=>x.invoice_id===invoice.id));}catch(error){showToast(error.message,TOAST_TYPES.ERROR);}}));
  document.querySelectorAll("[data-download-credit]").forEach((button)=>button.addEventListener("click",async()=>{try{const note=state.credits.find((x)=>x.id===button.dataset.downloadCredit);await downloadHospitalBillingPdf({...note,entityType:"hospital_credit_notes"},state.project,state.project.hospital_clients,[]);}catch(error){showToast(error.message,TOAST_TYPES.ERROR);}}));
  document.querySelectorAll("[data-delete-invoice],[data-delete-credit]").forEach((button)=>button.addEventListener("click",async()=>{const invoiceId=button.dataset.deleteInvoice,entityType=invoiceId?"hospital_invoices":"hospital_credit_notes",entityId=invoiceId||button.dataset.deleteCredit,row=invoiceId?state.invoices.find((x)=>x.id===entityId):state.credits.find((x)=>x.id===entityId),number=invoiceId?row?.invoice_number:row?.credit_note_number;if(!row||!confirm(`Permanently delete draft ${number} from EMS and the project Drive folder?`))return;try{await deleteHospitalBillingDocumentFromDrive(entityType,entityId);await logAuditEvent("hospital_billing_document_deleted",{moduleCode:MODULES.HOSPITAL_PROJECTS,entityType,entityId,beforeData:row,afterData:{hard_deleted:true,drive_deleted:true}});showToast(`${number} deleted from EMS and Google Drive.`,TOAST_TYPES.SUCCESS);await loadView();render();}catch(error){showToast(error.message,TOAST_TYPES.ERROR);}}));
  document.getElementById("openVendorBillUpload")?.addEventListener("click",()=>{state.vendorBillModal=true;render();});
  document.querySelectorAll("[data-close-vendor-bill-modal]").forEach((button)=>button.addEventListener("click",()=>{state.vendorBillModal=false;render();}));
  document.getElementById("projectVendorBillForm")?.addEventListener("submit", async (event) => {
    event.preventDefault();
    const form=event.currentTarget,file=form.elements.invoice_file?.files?.[0];
    if(!file)return showToast("Select the vendor invoice file.",TOAST_TYPES.ERROR);
    if(file.size>10*1024*1024)return showToast("Invoice file must be 10 MB or smaller.",TOAST_TYPES.ERROR);
    const submit=event.submitter;
    try {
      if(submit){submit.disabled=true;submit.textContent="Uploading…";}
      const payload=values(form),base64=await readBase64(file);
      const result=await uploadHospitalVendorInvoiceToDrive({
        projectId,vendorId:payload.vendor_id,vendorInvoiceNumber:payload.vendor_invoice_number,
        billDate:payload.bill_date,dueDate:payload.due_date,description:payload.description,
        taxableAmount:Number(payload.taxable_amount||0),taxRate:Number(payload.tax_rate||0),
        fileName:file.name,mimeType:file.type||"application/pdf"
      },base64);
      await logAuditEvent("hospital_vendor_invoice_uploaded",{moduleCode:MODULES.HOSPITAL_PROJECTS,entityType:"hospital_vendor_bills",entityId:result.bill?.id,afterData:{project_id:projectId,vendor_id:payload.vendor_id,vendor_invoice_number:payload.vendor_invoice_number,source:"staff",drive_file_id:result.fileId}});
      state.vendorBillModal=false;
      showToast(`${result.bill?.bill_number||"Vendor invoice"} uploaded and stored in the project Drive folder.`,TOAST_TYPES.SUCCESS);
      await loadView();render();
    } catch(error) {
      showToast(error.message,TOAST_TYPES.ERROR);
      if(submit){submit.disabled=false;submit.textContent="Upload Invoice";}
    }
  });
  document.getElementById("contractorForm")?.addEventListener("submit", async (event) => { event.preventDefault(); try { const payload=values(event.currentTarget);payload.project_id=projectId;payload.awarded_value=Number(payload.awarded_value||0);const {error}=await db.from("hospital_project_vendors").insert(payload);if(error)throw error;await logAuditEvent("hospital_contractor_assigned",{moduleCode:MODULES.HOSPITAL_PROJECTS,entityType:"hospital_projects",entityId:projectId,afterData:payload});showToast("Contractor assigned.",TOAST_TYPES.SUCCESS);await loadView();render(); } catch(error) { showToast(error.message,TOAST_TYPES.ERROR); } });
  document.getElementById("ensureDriveFolders")?.addEventListener("click", async () => { try { const result=await ensureHospitalProjectDriveFolders(projectId);state.project.drive_folder_status="ready";state.project.drive_folder_path=result.folderPath;showToast("Google Drive project folders are ready.",TOAST_TYPES.SUCCESS);render(); } catch(error) { showToast(error.message,TOAST_TYPES.ERROR); } });
  document.getElementById("documentForm")?.addEventListener("submit", async (event) => { event.preventDefault(); const form=event.currentTarget,files=[...form.elements.files.files];if(!files.length)return;try{for(const file of files){await uploadHospitalProjectDocumentToDrive({projectId,documentType:form.elements.document_type.value,fileName:file.name,mimeType:file.type||"application/octet-stream"},await readBase64(file));}showToast(`${files.length} document(s) uploaded to Google Drive.`,TOAST_TYPES.SUCCESS);await loadView();render();}catch(error){showToast(error.message,TOAST_TYPES.ERROR);} });
  document.querySelectorAll("[data-delete-project-document]").forEach((button)=>button.addEventListener("click",async()=>{const documentRow=state.documents.find((row)=>row.id===button.dataset.deleteProjectDocument);if(!documentRow||!confirm(`Delete “${documentRow.file_name}” from EMS and move the Google Drive file to trash?`))return;try{button.disabled=true;button.textContent="Deleting…";await deleteHospitalProjectDocumentFromDrive(documentRow.id);await logAuditEvent("hospital_project_document_deleted",{moduleCode:MODULES.HOSPITAL_PROJECTS,entityType:"drive_documents",entityId:documentRow.id,beforeData:documentRow,afterData:{deleted_at:new Date().toISOString(),drive_trashed:true}});showToast("Document deleted from EMS and moved to Google Drive trash.",TOAST_TYPES.SUCCESS);await loadView();render();}catch(error){showToast(error.message||"Document could not be deleted.",TOAST_TYPES.ERROR);button.disabled=false;button.textContent="Delete";}}));
  document.getElementById("settingsForm")?.addEventListener("submit", async (event) => { event.preventDefault();try{const form=event.currentTarget,payload=values(form),scopeCodes=[...form.querySelectorAll('input[name="scope_codes"]:checked')].map(x=>x.value);if(!scopeCodes.length)throw new Error("Select at least one service scope.");delete payload.scope_codes;payload.progress_percent=Number(payload.progress_percent||0);const {error}=await db.from("hospital_projects").update(payload).eq("id",projectId);if(error)throw error;const {error:scopeError}=await db.rpc("hospital_set_project_scopes",{p_project_id:projectId,p_scope_codes:scopeCodes});if(scopeError)throw scopeError;await logAuditEvent("hospital_project_settings_updated",{moduleCode:MODULES.HOSPITAL_PROJECTS,entityType:"hospital_projects",entityId:projectId,afterData:{...payload,scope_codes:scopeCodes}});showToast("Project settings updated.",TOAST_TYPES.SUCCESS);location.reload();}catch(error){showToast(error.message,TOAST_TYPES.ERROR);} });
}
function render() {
  document.body.querySelectorAll(":scope > .hp-billing-modal, :scope > .hp-native-pdf-modal, :scope > .hp-vendor-bill-modal").forEach((modal) => modal.remove());
  renderModuleContent(body());
  document.querySelectorAll(".hp-billing-modal, .hp-native-pdf-modal, .hp-vendor-bill-modal").forEach((modal) => document.body.append(modal));
  bind();
}

export async function initHospitalProjectWorkspace(view) {
  state.view = Object.hasOwn(PAGE_META, view) ? view : "overview";
  let project = null;
  try { project = await prefetchProject(); } catch (error) { console.warn("[HOSPITAL_PROJECT_PREFETCH_FAILED]", error); }
  const meta = PAGE_META[state.view];
  state.boot = await bootstrapProtectedPage({ moduleCode: MODULES.HOSPITAL_PROJECTS, pageTitle: project ? `${project.title} · ${meta[0]}` : meta[0], pageDescription: meta[1], workspace: WORKSPACES.HOSPITAL_PROJECTS, sidebarContext: project ? sidebarContext(project) : null });
  if (!state.boot) return;
  if (!projectId || !project) { renderModuleContent(`<section class="hp-card"><h3>Project unavailable</h3><p>Select a Hospital project from the project register.</p><a class="btn" href="${ROUTES.HOSPITAL_PROJECTS}">Back to Hospital Projects</a></section>`); return; }
  try { await loadView(); render(); } catch (error) { console.error("[HOSPITAL_PROJECT_WORKSPACE_FAILED]", error); showToast(error.message || "Project workspace failed to load", TOAST_TYPES.ERROR); }
}

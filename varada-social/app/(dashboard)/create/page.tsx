import { CreationStudio } from "@/components/create/creation-studio";

export const metadata = {
  title: "AI Creation Studio",
};

export default function CreatePage() {
  return <CreationStudio />;
}

# Inactive Supabase directory

This repository-root directory is intentionally **not** a Supabase project.

Use [`new-ems/supabase`](../new-ems/supabase) for configuration, migrations,
functions, linking, status checks, and deployments. Any SQL created under
`supabase/migrations` will fail the repository migration guard.
